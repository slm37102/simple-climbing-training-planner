# Grill queue

The **decision worklist**. Where `docs/improvement-backlog.md` (the `IB-*` ledger) is the full catalogue of everything the app should get better at, this file is the short, ordered list of items that are blocked on a **training-content decision** — the human-gated work that `/audit-loop` can find and file but structurally cannot build, because a change to prescription math must be decided (and reviewed) before it ships.

It exists because that work was the loop's dead end: `/audit-loop` drains the *engineering* backlog on its own, but every training item and every prescription-math trip-wire routes to the multi-session `/grill-with-docs → /to-spec → /to-tickets` path, and the first step of that path needs a live human. Rather than let those items sit as scattered "routed to multi-session" notes that never move, they collect here and get worked one at a time.

This is a lightweight, markdown-native cousin of the `/wayfinder` map: same philosophy (chart the decisions, resolve them one at a time until the way is clear), without the issue-tracker machinery. If this queue ever outgrows a flat list — real blocking edges, parallel sessions, fog that needs charting — graduate it to an actual `wayfinder:map` and retire this file.

## How to read this doc

- The queue below is **ordered by priority** (safety G3 > correctness G1/G2 > convenience > polish — the same order the backlog uses). The top unblocked row is the **frontier**: the next thing to grill.
- Each row is a **pointer**, not a write-up — the finding's detail lives in its `IB-*` backlog row and the source audit. The queue carries only: the ID, a one-line question, and status.
- This is **not a mirror of the training backlog.** Only items that are *ready to decide* live here. The rest stay in the backlog as the catalogue; `/audit-loop` and the user graduate them in as they rise to the frontier (wayfinder's "fog of war" — don't pre-slice what you can't yet phrase as a sharp question).
- **Status:** Queued / Grilling / Decided / Dropped.

## Coordination across sessions — the claim

Many sessions run at once (all yours), so a **claim** is what stops two of them grilling the same item. The claim lives on the **GitHub issue tracker**, not in this file — a markdown claim written on one session's branch is invisible to another until it merges, whereas an issue's comments are visible to every session instantly.

Every in-flight queue item is backed by a GitHub issue labelled **`grill-queue`**. Since every session is **you** (one GitHub user), an assignee would read the same on all of them, so it's skipped — the claim is simply a **comment**:

- **Claim = a `🔒 CLAIM` comment** naming your **Claude session id**, **branch**, and **UTC time**. An open `grill-queue` issue whose latest claim is an unresolved `🔒 CLAIM` is **taken**; one with no `🔒 CLAIM` (or only a stale one) is **free**.
- **Tiebreak:** right after commenting, re-read the issue; if an *earlier* `🔒 CLAIM` from a different session exists, **yield** and take the next item. Comment order is the lock.
- **Stale claims release.** A `🔒 CLAIM` with no follow-up for a few hours on a still-open issue is a dead session; reclaim it with a `♻️ RECLAIM` comment noting the takeover.

When an item is `Decided`, its issue is **closed** — off the board for everyone.

## How to work this queue — "run a grill section"

Invoke **`/grill-queue`** (or just say "grill the next item"). One item per session. The ritual follows `/grill-with-docs` = `/grilling` + `/domain-modeling`:

1. **Take the top claimable item** — the highest-priority `Queued` row whose issue is **open with no active `🔒 CLAIM`** (or the one the user names). **Claim it** per the section above (post a `🔒 CLAIM` comment; yield if an earlier claim exists). Then mark the row `Grilling`.
2. **Grill interactively — HITL.** One question at a time, each with a recommended answer, waiting for the user's answer before the next. Look **facts** up in the code and the `docs/` (training-philosophy, the ADRs, knowledge-gaps); put **decisions** to the user. The agent **never answers its own questions** — a grill where the agent supplies the human's side is broken. Ground every branch in the evidence the way `docs/adr/` already does.
3. **Produce a decision, not a deliverable.** The output of a grill is a settled call — what to build and why — not the code. Stop when there's shared understanding and nothing left to decide before someone builds it.
4. **Record it** (this is where a training finding **graduates**, per the repo convention):
   - Write the decision as a new **ADR** in `docs/adr/`, and add a **`KG-*`** row to `docs/knowledge-gaps.md` carrying the adjudicated verdict.
   - Mark the queue row **Decided**, linking the ADR.
   - Update the item's `IB-*` backlog row: it's now decided, so it's ready for the **build** path (`/to-spec → /to-tickets → PR`).
5. **Graduate fog.** If the decision makes a previously-vague backlog item phrasable as a sharp question, add it to the queue. If it reveals an item is out of scope, drop it (status `Dropped`, one-line why).

Grilling settles the *decision*; it does not ship code. Once an item is `Decided`, the build still runs as its own multi-session `/to-spec → /to-tickets` pass — but the hard, human-only part is done and captured durably.

## Queue

Ordered; top unblocked row is the frontier.

The **Issue** column is the claim surface — an open issue with no active `🔒 CLAIM` comment is claimable. Priority order; top claimable row is the frontier.

| ID | Decision to settle | P | Issue (claim here) | Status |
|----|--------------------|---|--------------------|--------|
| IB-064 | Should a hold week (deload/retest/taper/pain-amber) also clamp the readiness "Push" (×1.05 kg), not just the +2.5% targets-hit step? | P1 | [#76](https://github.com/slm37102/simple-climbing-training-planner/issues/76) | **Decided** → [ADR-0019](adr/0019-hold-weeks-clamp-readiness-push.md) |
| IB-029 | Should the autoregulation magnitudes self-tune from the athlete's logs, or stay honest-labeled invented defaults? | P2 | [#75](https://github.com/slm37102/simple-climbing-training-planner/issues/75) | **Grilling** (session ymswpe) |
| IB-020 | Finger norm table is a 7s hang but `maxHang20mm` is a 10s hold — retest at 7s, convert, or widen the threshold? | P2 | [#77](https://github.com/slm37102/simple-climbing-training-planner/issues/77) | Queued |
| IB-014 | What injury-history intake should exist, and how should it pre-soften finger load (vs. today's purely reactive moderation)? | P1 | [#60](https://github.com/slm37102/simple-climbing-training-planner/issues/60) | **Decided** → [ADR-0017](adr/0017-finger-tweak-return-state.md) |
| IB-024 | Should the antagonist block gain a vertical/scapular press, and what/where? | P1 | [#61](https://github.com/slm37102/simple-climbing-training-planner/issues/61) | **Decided** → [ADR-0018](adr/0018-vertical-press-in-antagonist-block.md) |
| IB-056 | Should taper weeks hold the +2.5% targets-hit progression? | P2 | [#68](https://github.com/slm37102/simple-climbing-training-planner/issues/68) | **Decided** → [ADR-0007 addendum](adr/0007-taper-hold-intensity-peaktype.md) |
| IB-058 | Should a day with no readiness check-in default to Normal or Lighter? | P2 | [#70](https://github.com/slm37102/simple-climbing-training-planner/issues/70) | **Decided** → [ADR-0015 addendum](adr/0015-readiness-gating-climbing-sessions.md) |
| IB-030 | Should the deload × readiness-Lighter volume stack have a combined floor? | P3 | [#69](https://github.com/slm37102/simple-climbing-training-planner/issues/69) | **Decided** → [ADR-0015 addendum](adr/0015-readiness-gating-climbing-sessions.md) |

## Decided

One line per resolved item — the ADR it produced, so the queue stays auditable and resumable.

- **IB-064** ([#76](https://github.com/slm37102/simple-climbing-training-planner/issues/76), 2026-08-06) — the readiness **"Push" (×1.05)** was the *second*, ungated upward lever: `resolveEffective` applies it unconditionally at `js/loads.js:210-213`, **outside** the `holdProgression` gate that suppresses the +2.5% step (KG-B14/B17), so a good-readiness check-in still raised finger/pull **kg +5%** on a deload / taper / pain-amber week — larger than the +2.5% the hold exists to stop, and Push is likeliest exactly on a deload (athlete freshest). Worst case is a **pain-amber** day that slept well: +5% finger load on a tweaked tendon (pain is finger-specific, s/s/f is general wellness, so they co-occur readily). Grilled to a clean **clamp**. The spine of the decision was a distinction found in the two prior addenda: both IB-028 and IB-056 deliberately *kept* the ±5% `autoAdjust` thermostat running on holds because it is **corrective** ("you under-loaded, come back *to* the held band"); the readiness Push is a different lever — a **reward** for feeling fresh that pushes *above* the band. Decision: **clamp readiness ≤1.0 on any hold** (`Math.min(readinessMultiplier, 1.0)` before `:210`), gated on `holdProgression` truthiness (cause-agnostic — mirrors the +2.5% suppression, inherits `taper` for free when IB-056 lands, `retest` a harmless no-op). Downward Lighter (×0.85) / rest (×0) pass through untouched; the corrective thermostat stays running. This completes a single doctrine across three decisions: **on any hold, suppress the reward-upward levers (+2.5% targets-hit, ×1.05 Push); keep the corrective RPE thermostat.** Clamp lives in `resolveEffective` (invariant holds by construction for every caller; co-located with the +2.5% suppression) not the callers (drift risk). Emits a phase-neutral "Push held" trail line so the user-visible "Why this load" badge accounts for both held levers. **Out of scope (deliberate):** making pain-amber go *lighter* — that's ADR-0014 / IB-025 / the ADR-0017 finger-tweak machinery, a separate larger call. Build scope: one `Math.min` clamp + one trail line in `resolveEffective` (trip-wire 2) + an `[IB-064]` regression; **no schema bump, no new flag, no `holdProgressionFor` change.** → [ADR-0019](adr/0019-hold-weeks-clamp-readiness-push.md), KG-B20. Ready for `/to-spec → /to-tickets`.
- **IB-058** ([#70](https://github.com/slm37102/simple-climbing-training-planner/issues/70), 2026-08-06) — a day with **no readiness check-in** was fabricated as `{3,3,3}` (avg 3.0 → Lighter ×0.85), silently under-dosing every session and freezing that fake value into the day record. Grilled to a clean **Normal**. Three code facts settled it against the "err lighter" counter-argument: (1) `computeReadinessMultiplier(null)` *already* returns Normal — the view fabricated `{3,3,3}` specifically to override its own no-data contract; (2) the scale's neutral point is **4**, not 3 (tests peg `{4,4,4}` as flat/normal and assert it → normal), so a fabricated 3 invents a mildly-sub-par report from silence; (3) `readinessScore` already returns `null` for absent/partial readiness, so the monitoring layer already treats no-check-in as no-data — today.js was the lone dissenter, and its persisted `{3,3,3}` polluted the `readinessTrend` baseline. Reframed the axis: this is **G1/G2 under-dosing**, not G3 — the s/s/f pills are subjective wellness, and the real injury gates (pain→Silbernagel, density guard, layoff decay) are separate and fire regardless, so "err lighter" bought no tissue protection. Decision: no check-in → **Normal** — pass absent readiness straight through (`computeReadinessMultiplier`'s guard owns it; pills render unfilled, distinguishing "not checked in" from "logged straight-3s"), and **stop persisting** `{3,3,3}` so the trend baseline stays honest. Thresholds left **untouched** — a *deliberately-logged* straight-3 still reads Lighter (correct autoregulation; realigning was rejected as out-of-scope, would retro-change every logged 3). Build scope: view-layer default fix (render L494 + persist L1078) + one no-check-in test; **no schema/load-chain change**. → [ADR-0015 addendum](adr/0015-readiness-gating-climbing-sessions.md), KG-B19. Ready for `/to-spec → /to-tickets`.
- **IB-030** ([#69](https://github.com/slm37102/simple-climbing-training-planner/issues/69), 2026-08-05) — the deload × readiness-Lighter double volume cut (~×0.51). Grilled past the audit's own framing on two counts found in the code: the "~×0.51" nominal understates the real worst case — `scaleTarget` floors twice, so limit boulders go **4 → 2 → 1 (×0.25)** and land the deepest cut on the RPE 9–9.5 limit/campus sessions; and the kg-chain analogy ADR-0015 rested on is false — deload never touches kg (ADR-0003), so the kg side already cuts *once*, making the climbing double-cut more aggressive than the chain it cited. Direction was never unsafe (both downward, no G3), so the call was taken on G1 + "simple over clever." Decision: **cut volume once** — the readiness gate skips its ×0.85 volume-scaling on any exercise a cut already stamped with `originalTarget` (pass-order-independent; correctly ignores the upward Base ramp's `rampedFrom`), while **keeping** the ≤8.5 RPE cap and the rest/lactic swaps and rewording the note to intensity-only (resolving the audit's two-note double-count nit for free). A **combined floor** (audit's alternative) was **rejected** — cross-pass magnitude state, more complex than a boolean guard, and still double-counts the axis. Build scope: one `applyReadinessLighter` guard + note reword + update `tests/cases/16-readiness-gating.js:128`; **no schema/load-chain change**. → [ADR-0015 addendum](adr/0015-readiness-gating-climbing-sessions.md), KG-B18. Ready for `/to-spec → /to-tickets`.
- **IB-056** ([#68](https://github.com/slm37102/simple-climbing-training-planner/issues/68), 2026-08-05) — the reachable twin of the IB-028 defect: taper weeks applied the +2.5% targets-hit step, contradicting the taper's own "intensity held" note, landing on the *final week before the goal* (trip/project) or cross-cycle (comp). Grilled to a clean **hold**, mirroring IB-028 exactly. Decision: suppress the +2.5% step on taper; leave the ±5% `autoAdjust` thermostat running (one capped, signal-driven nudge is autoregulation, not creep — same line the IB-028 addendum drew for deload). A code fact found during the grill collapsed the mechanism the issue anticipated: `ctx.phase` is already on the resolved context and already passed to `holdProgressionFor` at both `today.js` call sites, so the fix is a **one-line** `if (ctx?.phase === 'taper') return 'taper';` predicate + one `HOLD_PROGRESSION_REASONS` entry — **no `ctx.taper` flag, no `buildPhasePattern` change, no schema bump, no call-site change** (simpler than #68 guessed). Peak phase deliberately left progressing (a real building phase). → [ADR-0007 addendum](adr/0007-taper-hold-intensity-peaktype.md), KG-B17. Ready for `/to-spec → /to-tickets`.
- **IB-024** ([#61](https://github.com/slm37102/simple-climbing-training-planner/issues/61), 2026-08-05) — reclaimed a stale 2-day-old claim, then settled. The grill's first move was to test the backlog's **P1/G3** tag against the evidence and **retire it**: the verified finding rates the injury-prevention premise weak (no climber RCT, "imbalance causes injury" sparsely supported) and notes the RCT-adjacent cuff prehab is *already* covered (band cactus + Y-T-W). So the call was taken on the "improves outcomes for this athlete" axis, not durability — and taken **narrowly**. Decision: add **one vertical press (pike push-ups) to the Monday block only**, with the scapular/serratus work **folded in as a top-of-rep cue** rather than a separate card; **drop "Inverted rows"** (redundant pull inside a pull-heavy week — enacting the finding's "reclassify rows out") while keeping band cactus; lightly rename the display label to "S&C / press & shoulder block" but keep the structural `kind: 'antagonist-block'`. Two deliberate divergences from the finding, both argued in the ADR: **1×/week, not 2×/week** (the 2×/week case rests on the volume-distribution/injury premise the decision declines; Tuesday stays a clean prehab subset), and **no separate serratus item** (weakest-evidenced piece; the vertical press trains serratus in the relevant range). No schema/load-chain change; deload inherited via the existing block-item cut. → [ADR-0018](adr/0018-vertical-press-in-antagonist-block.md), KG-B16. **Spec'd 2026-08-05** as [#72](https://github.com/slm37102/simple-climbing-training-planner/issues/72) (`ready-for-agent`) — awaiting `/to-tickets → PR`.
- **IB-014** ([#60](https://github.com/slm37102/simple-climbing-training-planner/issues/60), 2026-08-03) — reframed during the grill: not a historical injury-trait intake (that branch was **dropped** — ADR-0001 already pre-softens categorically for this exact cohort, and a permanent flag would soften forever for tissue that has adapted) but a **declared, pain-gated finger-tweak return state**. Armed by a one-tap accept on the pain-red banner — which today has *no* teeth — behind a red-flag acknowledgement screen. Lowers finger intensity via a **ramping ceiling** (`min(chain, ceiling)`, applied last so armed can only ever reduce), regresses grip to open-hand, enforces the prose-only `skip on any finger tweak` campus gate, and **leaves volume alone** — deliberately inverting the repo's cut-volume/hold-intensity lever, because the evidence declines to quantify volume for a tweak and grip regression is the one VALIDATED mechanism. → [ADR-0017](adr/0017-finger-tweak-return-state.md), KG-B15. **Partially reverses** the 2026-07-17 KG-A7 doc-only call, argued explicitly in the ADR. Two code facts found during the grill drove the mechanism: a kg multiplier moves *total* load only ~2.6% (so it isn't a softening), and `resolveEffective` never clamps into `range` (so lowering a band alone does nothing once history exists). Left deliberately untouched for their own grills: IB-030 (#69) stacking floor, IB-058 (#70) missing-check-in default. Ready for `/to-spec → /to-tickets`.
- **IB-028** ([#59](https://github.com/slm37102/simple-climbing-training-planner/issues/59), 2026-07-30) — set the existing `holdProgression` flag on deload/retest weeks to suppress the +2.5% step; ±5% thermostat left running (minimal fix). → [ADR-0009 addendum](adr/0009-intra-phase-progression.md), KG-B14. **Built and merged 2026-07-31** — spec [#62](https://github.com/slm37102/simple-climbing-training-planner/issues/62) → [#63](https://github.com/slm37102/simple-climbing-training-planner/issues/63)/[PR #65](https://github.com/slm37102/simple-climbing-training-planner/pull/65) → [#64](https://github.com/slm37102/simple-climbing-training-planner/issues/64)/[PR #66](https://github.com/slm37102/simple-climbing-training-planner/pull/66). Two corrections to the decision came out of the build, both recorded as a second dated ADR-0009 addendum: the "no `js/loads.js` change" line no longer holds (IB-041 made the load-reason trail user-visible, so the hardcoded "pain amber" message had to go), and the retest leg of the rationale is **unreachable** — no retest-week session carries a kg-bearing exercise. Also spawned **IB-056**: taper weeks have the same defect and, unlike retest, it *is* reachable.
