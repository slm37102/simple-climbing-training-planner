# Improvement backlog

The **live** to-do ledger — what the app should get better at next, and what has been ruled out. Maintained by the `/audit-loop` skill, which surveys the code, files what it finds here, and drains one item per pass.

**How to read this doc**

- Every item has a stable ID (`IB-001`, …) so commits, issues and PRs can cite it ("closes IB-028"). IDs are **never reused and never deleted** — a finished item keeps its row with closure provenance, the same rule [`knowledge-gaps.md`](knowledge-gaps.md) uses for `KG-*`.
- Rows carry **pointers, not write-ups**. The reasoning lives in the source document named in the Source column — for the seeded rows that is [`deep-audit.md`](deep-audit.md), by dimension section (`§9` = its `## 9. Software Design`).
- **Kind** routes the work. `eng` = fixable by reading code alone (the audit's own `basis: software-only`). `training` = needs a training-content decision first; when one is made it **graduates** to a `KG-*` row in [`knowledge-gaps.md`](knowledge-gaps.md) plus an ADR, per this repo's decision-log convention. Raw findings are not KG entries — a KG row carries an adjudicated *Verdict*, and nothing here has been adjudicated yet.
- **Priority** re-derives the audit's own High/Medium/Low under [`project-goals.md`](project-goals.md)'s order — **safety (G3) > prescription correctness (G1/G2) > convenience > polish**. The mapping is mechanical so it stays auditable: **P1** = the audit's High, or an item on its "Highest injury risks" list · **P2** = its Medium · **P3** = its Low. Per `CLAUDE.md`, implementation effort is **not** an input to priority.
- **Status:** Open / Researching / Closed / Won't-fix.
- Engineering-quality items live here. Training-content *gaps* — once decided — live in [`knowledge-gaps.md`](knowledge-gaps.md). The 2026-06-24 [`improvement-audit.md`](improvement-audit.md) and the 2026-07-14 [`coach-review.md`](coach-review.md) are historical records, not trackers.

**Inherited caveats.** `deep-audit.md` is honest about two weaknesses of its own, and they travel with the rows seeded from it: **IB-033** never completed adversarial verification (the run hit a usage limit), and one evidence subagent was flagged for a security-policy violation while working the hangboard-protocol topic, so external citations in that area deserve extra skepticism. Code citations are independently checkable and unaffected. The audit's priorities are the auditor's, not a ratified plan — every row is re-verified against current code when the loop picks it up, not at import time.

## Frontier

Highest-priority open decisions. None is built by `/audit-loop` inline — each lives in the **[grill queue](grill-queue.md)** and is worked with **`/grill-queue`** (interactive grilling → ADR + `KG-*` decision → build path). `/audit-loop` itself drains the highest-priority *inline-buildable* item each pass; when the top item is a not-yet-enqueued trip-wire (as IB-056 was), the pass **enqueues** it here instead. **IB-028 cleared this list on 2026-07-31** — decided, spec'd, built and merged (PRs #65 + #66).

- **IB-024** (P1, training) — antagonist block is pull-dominant with no vertical/scapular press. Queued for grilling ([#61](https://github.com/slm37102/simple-climbing-training-planner/issues/61)).
- **IB-064** (P1, G3, eng) — a recovery/hold week suppresses the +2.5% step (IB-028/056) but **not** the readiness "Push" (×1.05), which still raises finger/pull kg +5% on deload/retest/taper/pain-amber weeks. The IB-056 decision (2026-08-05) deferred this facet by design. **Enqueued pass 31, 2026-08-06** ([#76](https://github.com/slm37102/simple-climbing-training-planner/issues/76)).
- **IB-056** (P2, G3, eng) — taper weeks still ratchet finger load +2.5% on the pre-goal week; same defect IB-028 fixed for deload/retest, but reachable. The fix is trip-wired *and* the "should a taper hold or creep?" call is training-content, so it needs a decision first. **Enqueued pass 10, 2026-08-02** ([#68](https://github.com/slm37102/simple-climbing-training-planner/issues/68)).

**IB-014 left this list on 2026-08-03** — decided as [ADR-0017](adr/0017-finger-tweak-return-state.md) / KG-B15 (grilled via [#60](https://github.com/slm37102/simple-climbing-training-planner/issues/60)), awaiting `/to-spec → /to-tickets`.

## Items

| ID | Finding | Kind | P | Goals | Status | Source |
|----|---------|------|---|-------|--------|--------|
| IB-014 | No injury-history intake — finger loading is only ever moderated reactively, never pre-softened. **Reframed by the grill (2026-08-03):** the audit's "historical injury trait" branch was **dropped** (ADR-0001 already pre-softens categorically for this cohort; a permanent flag softens forever for adapted tissue; the current-ability half is IB-013). What ships instead is a **live finger-tweak return state** — the real hole, since layoff decay keys on time *off* and an athlete who tweaks but keeps training gets nothing, while pain-red is text-only (`signalHasAccept` covers `early-deload` alone). Two code facts constrain the build: a kg multiplier moves *total* load only ~2.6% (ADR-0013 units), and `resolveEffective` never clamps into `range`, so a lowered band is inert once history exists — the softening must be a **total-load ceiling clamp applied last** | training | P1 | G3 | **Decided 2026-08-03** ([ADR-0017](adr/0017-finger-tweak-return-state.md) + KG-B15, grilled via [#60](https://github.com/slm37102/simple-climbing-training-planner/issues/60)) — ready for `/to-spec → /to-tickets` | [deep-audit §5](deep-audit.md) |
| IB-024 | Antagonist block is pull-dominant — one pressing movement, no vertical/scapular press | training | P1 | G3 | Open | [deep-audit §7](deep-audit.md) |
| IB-028 | Deload weeks silently progress finger/pull load +2.5%, contradicting the "deload = intensity held" invariant. **Re-verified 2026-07-29:** the suppression flag already exists and works — `resolveEffective`'s `holdProgression` (ADR-0014) — but is set only from amber pain (`today.js:973`, `:1122`), never from deload/retest. The fix is to also set that flag, not to thread a new one. **Correction 2026-07-30 (`/to-spec`):** "`js/loads.js` need not change" holds for the suppression *logic* but not for the *explanation* — the trail hardcodes "pain amber — progression held", which IB-041 made user-visible, so a deload week would tell a pain-free athlete their pain was amber | eng | P1 | G3 | **Closed 2026-07-31** — decided ([ADR-0009 addendum](adr/0009-intra-phase-progression.md) + KG-B14, grilled via #59) → spec [#62](https://github.com/slm37102/simple-climbing-training-planner/issues/62) → built as [#63](https://github.com/slm37102/simple-climbing-training-planner/issues/63)/[PR #65](https://github.com/slm37102/simple-climbing-training-planner/pull/65) (prefactor: cause-carrying flag) + [#64](https://github.com/slm37102/simple-climbing-training-planner/issues/64)/[PR #66](https://github.com/slm37102/simple-climbing-training-planner/pull/66) (`Loads.holdProgressionFor`, both call sites). Deload weeks now hold; the ±5% thermostat still runs. Spawned **IB-056** (same defect on taper, reachable) and a second ADR-0009 addendum correcting the retest rationale | [deep-audit §8](deep-audit.md) |
| IB-007 | ADR-0006's ≥72h high-intensity guardrail never enforced; Peak stacks its two hardest sessions 48h apart | training | P1 | G3 | Closed ([ADR-0016](adr/0016-weekly-finger-density-guard.md), 2026-07-25) | [deep-audit §3](deep-audit.md) |
| IB-023 | Same ≥72h guardrail, seen from the injury-prevention lens | training | P1 | G3 | Closed ([ADR-0016](adr/0016-weekly-finger-density-guard.md), 2026-07-25) | [deep-audit §7](deep-audit.md) |
| IB-001 | The advertised Lattice 80/20 polarization is not what the prescriptions deliver — the plan is concurrent/threshold. **Re-kinded `training` → `eng` on pickup (pass 37), and that was the pass's whole realisation:** the row's `training` kind implied a decision was owed, but [deep-audit §1](deep-audit.md)'s Recommendation *already adjudicated the direction* and its adversarial verifier confirmed it — "a documentation over-claim, not an unsafe prescription", fix the label and **"do NOT take the alternative of lowering Base-phase RPE to fit the 80/20 label… bending the prescription to the label would be bad coaching"**, with "no training cost to relabelling". Both branches point one way, so nothing was left to decide and the actionable fix is docs-only | eng | P2 | G1 | Closed (audit-loop pass 37, 2026-08-06) — rewrote `docs/training-philosophy.md` §"Year-level shape" (now "Year-level shape and **intensity distribution**"). Every citation re-verified against current code first: the doc's *phase-duration* claim is **exactly true** (12-wk comp = 6/3/2/1 = 50/25/17/8%, probed via `buildPhasePattern`) and is kept; what was false was mapping that onto an *intensity* distribution. The section now states the model honestly as **concurrent / threshold-pyramidal** with a genuinely polarized *aerobic sub-component*, naming both rungs from source: easy = `sat-arc` RPE 4–6 / flash pyramid RPE 6–7.5 / optional open-climb RPE 4–6 (Base Saturdays **alternate** between the two easy rungs — verified by walking every Base Saturday, not assumed); threshold-to-hard = Base intro max hangs RPE 8–9 @ 80–85% total load, Base weighted pull-ups RPE 7–8.5, Base Thu projecting RPE 7.5–9, and the explicit `SIXTY_SIXTY_EXERCISE` RPE 7–8.5 rung a polarized model avoids. Quantified the crux concretely: a Base week's three loaded sessions are **one easy + two reaching RPE 8–9**, i.e. ~half hard, not 80% easy. **Prescriptions deliberately untouched** — and the doc now carries the audit's reader-hazard warning explicitly ("do not add hard work to 'fill the 20%'"), which is the actual G1/G3 risk the mislabel created. Bonus evidence found while checking for wider drift: the repo's own [`research/deep-research-report.md`](research/deep-research-report.md) Open Questions says polarized models "lack climbing-specific research" — cited, so 80/20 reads as the better-known label, not the better-evidenced choice. **Ratchet 4 clear:** no ADR asserts 80/20 (grepped `docs/adr/`), and nothing in `js/` consumes the label (`grep -riE "80/20\|polari" js/` → zero) — so no ADR addendum was owed. **No `KG-*` graduation owed either**, stated explicitly so it doesn't read as a skipped step: a KG row carries an *adjudicated training verdict*, and no prescription changed here. Drift confined to the one section — swept the whole repo; the only other hits are the research report (describing the literature, correct) and dated audit records (left as-is). Left as a bounded future trigger, not a fog row: if this athlete's frequency ever rises above 3×/week, "should the plan actually be polarized?" reopens. **Docs-only** diff (no `js`/`css`/`sw.js`/`tests`) → ratchet 1 trivial | [deep-audit §1](deep-audit.md) |
| IB-002 | 8-week trip/project cycle degenerates to a single hard Base week before the retest | training | P2 | G1, G2 | Open | [deep-audit §2](deep-audit.md) |
| IB-003 | ADR-0002 still documents the pre-ADR-0004/0007 phase-derivation formula (`%3` cadence, weeks≥14 taper) | eng | P2 | — | Closed ([ADR-0002 addendum](adr/0002-configurable-cycle-length.md), audit-loop pass 3, 2026-07-29) | [deep-audit §2](deep-audit.md) |
| IB-008 | Hybrid Build provides almost no lead/route-specific climbing, under-serving the 7b goal | training | P2 | G1 | Open | [deep-audit §3](deep-audit.md) |
| IB-009 | Dedicated progressive-overload finger training is only 1×/week, below the 2×/week the philosophy calls the #1 grade lever | training | P2 | G1 | Open | [deep-audit §4](deep-audit.md) |
| IB-013 | Prescription structure never scales to current ability — only target grade and discipline are captured | training | P2 | G1 | Open | [deep-audit §5](deep-audit.md) |
| IB-018 | Power, contact strength and coordination are thin for the actual (hybrid) athlete | training | P2 | G1 | Open | [deep-audit §6](deep-audit.md) |
| IB-019 | All fingerboard work is 20mm half-crimp/open-crimp — no pinch, sloper or drag | training | P2 | G1 | Open | [deep-audit §6](deep-audit.md) |
| IB-020 | Limiter readout compares a 10-second benchmark against a 7-second norm table | training | P2 | G1 | Open | [deep-audit §6](deep-audit.md) |
| IB-029 | The autoregulation magnitudes (±5% RPE step, readiness ×0.85/1.05, −3%/wk decay, +2.5%, +5% cap) are app-invented and unvalidated. **Re-verified 2026-08-05:** the finding splits in two. The **honest-labeling** half is already closed — KG-C7 (disclosed 2026-07-23); `loads.js:17-22`/`:42-46`/`:255-256` carry "APP CONVENTION, UNVALIDATED" comments and `TARGETS_HIT_PROGRESS=1.025`/`MAX_SESSION_PROGRESS=1.05` (`loads.js:303-304`). What remains is the **self-calibration** loop those comments themselves promise (`loads.js:21-22` "Tune them from the athlete's own logs via the KG-A4 monitoring signals", repeated `:46`) but never built — `monitoring.js` only surfaces advisory signals, nothing fits the constants to this athlete. The audit's own trade-off: self-tuning adds real complexity and needs enough logged history to be stable. Separable sub-question: the sub-maximal regime (ARC/60-60) is governed by the same ±5%/+2.5% thermostat despite RPE being a noisy input below near-failure — suppress/widen there? Residual concern is coaching depth (G1) + over-trust, not injury (G3, conservative by construction). Decision-gated (build self-calibration vs. keep honest defaults) **and** trip-wire (closing the loop touches `js/loads.js` = trip-wire 2), so not inline | training | P2 | G1, G3 | Open — **Enqueued for grilling 2026-08-05 (audit-loop pass 28)**, [grill-queue #75](https://github.com/slm37102/simple-climbing-training-planner/issues/75), row in [grill-queue.md](grill-queue.md) | [deep-audit §8](deep-audit.md) |
| IB-032 | Domain logic leaked into the Today view — micro-retest staleness and readiness-label mapping | eng | P2 | — | Closed (audit-loop pass 6, 2026-07-30) — `Warmup.isMicroRetestDue({ctx, benchmarks, dateISO})` + `Program.readinessGateLabel(key)`; the second turned out to hide a real defect, not just cosmetics (see pass log) | [deep-audit §9](deep-audit.md) |
| IB-033 | Inconsistent magic-number discipline — build fraction `0.33` inlined at three sites, `densityRest` params, double-block threshold. **Re-verified 2026-07-30** (this row carried the audit's *unverified* caveat): two claims hold, one was already false at import time. `0.33` *was* inlined at three derivation sites (`_singleBlock` ×1, `_doubleBlock` ×2) and `densityRest` carried five unnamed numbers — including a `4` and a `3` on adjacent lines that are the *same* window length expressed two ways, the one spot in the cluster where drift could actually mis-prescribe. But `DOUBLE_BLOCK_THRESHOLD` was already a named constant with a rationale comment, so the third claim was wrong | eng | P2 | — | Closed (audit-loop pass 7, 2026-07-30) — named `BUILD_FRACTION_OF_REMAINING` + the `DENSITY_*` cluster (window/step/base/floor) and collapsed the `4`/`3` pair. **Trip-wire 1 disposal**, since the change edits `js/program.js`: the edited sites are `_singleBlock`/`_doubleBlock` (phase-pattern derivation, returning week arrays) and `densityRest` (a protocol helper) — none is a session builder and none is in `PRESCRIPTION_PASSES`; on the clause's *other* limb, a phase-split share and a rest-interval second are neither a prescription percentage nor an RPE, the values are unchanged, and the regenerated schedule is byte-identical, so nothing a human would need to decide moved. **Partial:** the redundant `densityRest(weeksLeft, 240)` argument *is* trip-wired — those three lines sit inside `buildThuMain`/`buildSatMain` — so it is split out as **IB-053** rather than silently left | [deep-audit §9](deep-audit.md) |
| IB-038 | The advertised uniform "3:1 / every 4th week" cadence is not what the generator produces at the retest seam | training | P2 | G1 | Open | [deep-audit §10](deep-audit.md) |
| IB-041 | Suggested load is shown with no explanation — `reason[]` is computed, never rendered, and comments claim a tooltip that does not exist | eng | P2 | G1, G3 | Closed (audit-loop pass 4, 2026-07-29) | [deep-audit §11](deep-audit.md) |
| IB-042 | Internal jargon "flavor" is surfaced to the athlete unexplained | eng | P2 | — | Closed (audit-loop pass 5, 2026-07-29) | [deep-audit §11](deep-audit.md) |
| IB-044 | Plateau detection is limited to strength retests and never sees climbing-grade progress | training | P2 | G1 | Open | [deep-audit §12](deep-audit.md) |
| IB-050 | `CLAUDE.md`'s load-chain invariants omitted `holdProgression` (ADR-0014) and asserted `resolveEffective` "has no deload parameter at all" — literally true of a *multiplier*, but it hid the flag that makes IB-028 a one-expression fix and would send a reader off building a new one | eng | P2 | G3 | Closed (audit-loop pass 1, 2026-07-29) | audit-loop L2 |
| IB-052 | The Today flavor badge can *contradict* the prescribed session, not just be unexplained: `ctx.flavor` resolves to the raw week-alternation (`focus==='hybrid' ? weekFlavor(weekIdx) : focus`, `program.js:1265`), so on a hybrid Build even/sport week the badge reads "sport" while Thursday content is force-overridden to limit bouldering (`hybridBuildMix`, `program.js:1274`). IB-042 added a tooltip caveat ("a given session may not match the label") but did not reconcile the value. Fix = make the badge reflect the session actually prescribed — a `js/program.js` change (trip-wire 1), so multi-session | eng | P3 | — | Closed (audit-loop pass 24, 2026-08-03, **user-authorized trip-wire auto-merge**) — root cause: the badge read the **week-level** `ctx.flavor` (`weekFlavor(weekIdx)`), but the session content follows `styleFlavor`, which `hybridBuildMix` (ADR-0010/KG-A10) pulls to `boulder` on a hybrid Build sport-parity Thursday. `session.flavor` could **not** just be repointed — it is a deliberate week-level invariant, pinned by `[Phase1 C1]` ("flavor constant across the week") and read by `generate-schedule.mjs` as "\<flavor\>-flavor week". So `finishSession` now also exposes the **day-level** `session.styleFlavor` (new display field, alongside the unchanged week `flavor`), and the Today badge reads `session.styleFlavor ?? ctx.flavor` — naming the session actually prescribed. Also retired IB-042's now-false "a given session may not match the label" tooltip caveat (it now matches). No domain logic moved into the view (it reads a domain-computed field — respects IB-032). **Prescription-neutral**: schedule regenerated **byte-identical**; two `[IB-052]` cases pin `styleFlavor=boulder`/week-`flavor=sport` on the wk8 Thursday + the badge reading "boulder"; the `[IB-042]` tooltip case updated to the reworded title. Suite **254/0**, `sw.js`→v68 | [deep-audit §11](deep-audit.md), split from IB-042 |
| IB-051 | `/audit-loop` presumes handoffs it cannot make. Five of the skills it delegates to are `disable-model-invocation: true` — `/implement`, `/grill-with-docs`, `/to-spec`, `/to-tickets`, `/improve-codebase-architecture` — so an agent cannot invoke them and stages 1/3 stall on a trip-wire item (`/tdd`, `/code-review`, `/test`, `/diagnosing-bugs` are reachable). Stage 4's unconditional "mint a GitHub issue" has the same root: its stated purpose is to give `/implement` a ticket, so it is noise on a docs-only pass done inline. Fix: have the skill do what those describe, or hand back for the user to type them, and make the issue conditional on a real handoff | eng | P2 | — | Closed (audit-loop pass 2, 2026-07-29) | audit-loop, pass 1 |
| IB-004 | `peakType: 'project'` is a distinct UI choice with no distinct behaviour — the Profile label promises a "rolling" taper never built | eng | P3 | — | Closed (audit-loop pass 15, 2026-08-02) — took the eng-fixable half: the false promise. `"rolling"` appeared in exactly one place (`profile.js:202`) and no rolling behaviour exists — `taperWeeksFor` and `buildPhasePattern` treat `trip`/`project` identically (2-wk taper), which the onboarding hint already states honestly. Relabelled to `"Project — 2-wk taper (open window)"` (parallel to Trip, matching onboarding's "riding the peak window"). Added `[IB-004]` pinning `buildPhasePattern(w,'project') === (w,'trip')` across cycle lengths, so if project ever *gains* distinct behaviour the stale label is caught. The deeper question — *should* project have its own rolling taper, or should the two options merge — is a product/training call, not this eng defect; left as a possible future decision, not blocking. Suite **248/0**, `sw.js`→v64 | [deep-audit §2](deep-audit.md) |
| IB-005 | 3:1 deload cadence degrades to an isolated single hard week before the Base retest | training | P3 | G1 | Open | [deep-audit §2](deep-audit.md) |
| IB-006 | Inter-cycle progression rests entirely on benchmark retest → load autoscaling; structure never advances | training | P3 | G1 | Open | [deep-audit §2](deep-audit.md) |
| IB-010 | Base boulder Thursday prescription is internally muddled | training | P3 | G1 | Open | [deep-audit §4](deep-audit.md) |
| IB-011 | ADR-0006's density progression is barely realized and the ADR overstates what it accomplishes | training | P3 | — | Open | [deep-audit §4](deep-audit.md) |
| IB-012 | Monday warm-up ramps only to 80% before near-max hangs; posterior-cuff dosing is thinner than claimed | training | P3 | G3 | Open | [deep-audit §4](deep-audit.md) |
| IB-015 | Anti-style cue is hard-coded to a crimp/overhang specialist — the personalization fields have no editor (KG-A10 closed but unusable) | eng | P3 | G1 | Closed (audit-loop pass 29, 2026-08-05) — `benchmarks.dominantStyle`/`dominantAngle` were schema-collected (defaults `crimp`/`slight-overhang`, `storage.js:46-47`) and read by the prescription (`buildAntiStyleCue`/`attachStyleNote`, KG-A10) but had **no editor**, so the anti-style cue was permanently pinned to the crimp/slight-overhang default — the "closed but unusable" gap. Added pill selectors to the Profile **Benchmarks** card (`js/views/profile.js`): read-only shows "strongest style" (e.g. `crimps · slight overhang`), Edit reveals grip (crimp/pocket/sloper/pinch) + angle (slab/vert/slight-OH/steep/roof) `.pill-group`s, committed on Done via the existing `Storage.setGlobalBenchmarks` (a merge, so the two fields join bodyweight/max-hang/1RM without disturbing them). **View-only — no trip-wire:** no `program.js`/`loads.js` edit (the fields were already read); the editor only realizes decided KG-A10 behaviour. `[IB-015]` regression pins the full path — mount Profile → Edit → pick sloper/roof → Done persists to `globalBenchmarks` → `prescribeForContext(...).styleNote` flips to the sloper/roof opposite (`crimps/pockets, slab or vertical`), proving the previously-unreachable cue is now reachable. Suite **257/0** (256 + 1), `sw.js`→`climb-planner-8fb6f7813406` | [deep-audit §5](deep-audit.md) |
| IB-016 | No equipment constraints — the plan assumes hangboard, campus board, lead wall and boulder wall are all available | training | P3 | — | Open | [deep-audit §5](deep-audit.md) |
| IB-017 | Limiter is detected but deliberately never feeds the plan — strengths/weaknesses change no prescription | training | P3 | G1 | Open | [deep-audit §5](deep-audit.md) |
| IB-021 | Lock-off and single-arm/max pulling strength are not developed | training | P3 | G1 | Open | [deep-audit §6](deep-audit.md) |
| IB-022 | Technique and tactics reach the athlete only as optional, non-progressed drill text | training | P3 | G1 | Open | [deep-audit §6](deep-audit.md) |
| IB-025 | Fingers never receive an intensity deload; deload/retest weeks hold near-max hangboard load | training | P3 | G3 | Open | [deep-audit §7](deep-audit.md) |
| IB-026 | Default 12-week cycle runs five consecutive loaded weeks into a single-week comp taper | training | P3 | G2, G3 | Open | [deep-audit §7](deep-audit.md) |
| IB-027 | No cumulative finger-load ceiling; the +5% cap is per-session only while hangboard recurs weekly | training | P3 | G3 | Open | [deep-audit §7](deep-audit.md) |
| IB-030 | Deload cut and readiness-"Lighter" scaling stack multiplicatively on climbing volume with no combined floor (~×0.51). **Enqueued for grilling 2026-08-02 (audit-loop pass 16)** — [grill-queue #69](https://github.com/slm37102/simple-climbing-training-planner/issues/69), row in [grill-queue.md](grill-queue.md). **Re-verified against current code:** `DELOAD_VOLUME_MULTIPLIER=0.6` and `READINESS_LIGHTER_MULTIPLIER=0.85` both scale climbing `prescribedTarget` via `scaleTarget`; the `PRESCRIPTION_PASSES` header says deload + Lighter are *not* mutually exclusive (ADR-0015) and the deload cut runs before the readiness gate (last), so the gate scales the already-cut target → `originalTarget`=full, `readinessScaledFrom`=×0.6 value, final ×0.51, no floor. Decision-gated (is a floor wanted, what value?) **and** trip-wire (a `PRESCRIPTION_PASSES` change), so not inline. Stays **Open** — enqueuing routes the decision | eng | P3 | G1 | Open | [deep-audit §8](deep-audit.md) |
| IB-031 | RPE-drift monitoring signal and the `autoAdjust` thermostat react to the same input at cross purposes | eng | P3 | — | Won't-fix (audit-loop pass 18, 2026-08-02) — **not a defect; the "cross purposes" framing is overstated**, as the deep-audit's own adversarial verifier already found (§8: "PARTIALLY_CONFIRMED, defect framing overstated"). Re-verified against current code: `autoAdjust` returns ×0.95 when prior avg RPE > range top (`loads.js:46`), while `rpeDriftSignal` fires only on **two consecutive** above-range hangboard/pullup sessions with `kgNotIncreased = latest.kg <= prev.kg` (`monitoring.js:82-85`) and escalates to "confirm with a micro-retest before adjusting anything." These are **intentional layered design**, not a conflict: `monitoring.js:1-8` documents the four signals as "reactive exception-catcher signals **layered on top of** the already-implemented proactive system," so the thermostat does the routine ±5% nudge and the drift signal escalates to an ADR-0012 micro-retest only after two failed self-corrections. `kgNotIncreased` screens out the benign "RPE high because load was pushed up" case, and the thermostat's own reason line points to the **same** micro-retest (`loads.js:177`) — so the two systems **converge**. No code change; closing as adjudicated-not-a-defect stops it re-surfacing | [deep-audit §8](deep-audit.md) |
| IB-034 | Coaching knowledge is hardcoded as branching JS in `program.js`, not editable structured data | eng | P3 | — | Open | [deep-audit §9](deep-audit.md) |
| IB-035 | Test suite is browser-only with no CLI runner; sync, charts and service worker partially uncovered | eng | P3 | — | Closed (audit-loop pass 30, 2026-08-05, **user-authorized**; the "no test framework" principle question was put to the user, who OK'd a dev/CI runner) — added **`tools/run-tests.mjs`**: serves the repo, loads `/tests/` in headless Chromium via Playwright, waits for the harness `#summary`, prints `N passed, M failed` + any failing case names, exits **0 green / 1 fail**. Faithful to the ethos: **not a test framework** — `tests/` + `harness.js` are untouched, it only *drives* the existing suite; the app keeps zero runtime deps (Playwright is an optional dev/CI tool the runner resolves from a local or global install, with an explicit install message + non-zero exit if absent). Proven both ways: green → `257/0` exit 0; a deliberately-failing case → exit 1 with the case named. Updated the "no CLI runner" claims in `CLAUDE.md` + the `test` skill. **Coverage sub-clause disposition:** *sync* is now well-covered (IB-059 `mergeBenchmarkHistory`, IB-061 migration chain, existing LWW cases); *charts* — there is no dedicated chart module (the grep hits are incidental `svg`/feed markup), so nothing to cover; *service-worker caching* (`sw.js` runtime) stays uncovered — generated code that's awkward to exercise in the DOM harness, low-value, available as a future L6 pick if wanted. Diff touches only `tools/` + docs (no `js`/`css`/`sw.js`/`tests`), so ratchet 1 is trivial; the runner itself was exercised to 257/0 | [deep-audit §9](deep-audit.md) |
| IB-036 | `program.js` and `today.js` concentrate mixed responsibilities in single ~1300-line files | eng | P3 | — | Open | [deep-audit §9](deep-audit.md) |
| IB-037 | Benchmark grade fields default to inconsistent empty types (`null` vs `''`) across code paths | eng | P3 | — | Closed (audit-loop pass 14, 2026-08-02) — the live `globalBenchmarks` path, `migrate`, and `mergeRemote` all use `''` for `sportGrade`/`boulderGrade`; only `defaultBenchmarks()` (`storage.js:42-43`, the per-plan legacy shape) and `onboarding.js:417`'s `?? null` used `null`. Grades are strings, so `''` is canonical (numeric benchmarks correctly stay `null`) — normalised both `null` sites to `''`. Safe: `normalizeGrade` treats both as empty (`if (!g)`), no code does a strict `=== null`/`=== ''` grade check, and grades aren't interpolated raw (so no "null" ever rendered). No `SCHEMA_VERSION` bump — the change only sets new defaults; existing stored `null`s stay harmless (consumers are null/''-indifferent), so no migration is owed. Added `[IB-037]` pinning that a fresh state's global **and** per-plan grade empties are both `''` while `maxHang20mm` stays `null`. Suite **247/0**, `sw.js`→v63 | [deep-audit §9](deep-audit.md) |
| IB-039 | Weighted pull-up %-bands carry the same load-precision framing as finger bands despite being non-climbing-specific convention | training | P3 | — | Open | [deep-audit §10](deep-audit.md) |
| IB-040 | Double-block threshold (20 wk) and the 1/3 build split are app-invented numbers presented with tri-source "consensus" framing | training | P3 | — | Open | [deep-audit §10](deep-audit.md) |
| IB-043 | Target-provenance callout can render dense, ambiguous phrasing on stacked-pass days | eng | P3 | — | Closed (audit-loop pass 22, 2026-08-03) — deep-audit §11 had already narrowed the residue: the **readiness** and **ramp** branches of `targetCalloutHtml` lead with the final value and dim provenance at `opacity:.6`, but the **deload branch** alone rendered `<s>original</s>value` inline at full opacity, original-first. Applied the sibling pattern — cut value is the bold headline, struck-through original demoted into a dimmed `opacity:.6` span. View-only (`js/views/today.js`), no trip-wire. `[IB-043]` regression pins value-first ordering + the dimmed strikethrough (fails on the old markup); suite **250/0** | [deep-audit §11](deep-audit.md) |
| IB-045 | Injury return-to-load is a doc link, not an in-app graded protocol | training | P3 | G3 | Open | [deep-audit §12](deep-audit.md) |
| IB-046 | Only one exercise substitution exists — no swap for equipment, injury or preference | training | P3 | — | Open | [deep-audit §12](deep-audit.md) |
| IB-047 | Q1 partial — `js/dates.js` exists and `storage.js` uses it, but the views still carry local date-helper copies | eng | P3 | — | Closed (audit-loop pass 12, 2026-08-02) — swept all of `js/` for date-math reimplementations; the two genuine `daysBetween` copies in the **views** (IB-047's scope) were `Math.floor((d − start)/86400000)` at `today.js:130` (cycle-stats day index) and `Math.round((start − today)/86400000)` at `profile.js:395` (comp-date hint). Both now call `daysBetween` (arg order `(from, to)`), standardising the profile hint on the app-wide **floor** convention — behaviour-identical except a theoretical ±1 across a DST boundary in a future-dated hint string. The remaining view sites are **not** helper copies: the calendar/onboarding/profile day-cursor loops (`setDate(getDate()+1)`) need the `Date` object per cell and already use `localIso`/`mondayDow` from `dates.js`, and the `new Date(iso+'T00:00:00')` parses are inside human-readable **formatters**, which CLAUDE.md keeps view-local. `daysBetween` semantics already pinned by `[Phase4 Q1]`; added `[IB-047]` guarding the floor/arg-order the migrated sites now depend on. Suite **246/0**, `sw.js`→v61. **Spun off IB-057** for the same inline copy in `Program.resolveDate` (core, not a view) | [improvement-audit](improvement-audit.md) Q1 |
| IB-048 | `.github/copilot-instructions.md` has drifted from `CLAUDE.md` — `buildPhasePattern` without `peakType`, taper length without the ADR-0007 derivation, no KG-B10 Monday-only retest correction, cites a removed Log edit-Save test | eng | P3 | — | Closed (audit-loop pass 11, 2026-08-02) — re-verified against code that the drift was **wider** than the four named items and fixed all of it: `buildPhasePattern(weeks, peakType)` + `taperWeeksFor` (2 trip/project, 1 comp, ADR-0007); retest volume-cut exemption is **Monday-only** (`env.slot==='mon-main'`, KG-B10); the removed Log-edit-Save test → Log renders read-only; **plus** `SCHEMA_VERSION` 5→6 (+ the missing v6 retest-history/`saveRetestBenchmarks` append, ADR-0014); the full ADR-0009/0013/0014 load chain (layoffDecay → targets-hit +2.5% → `holdProgression` → readiness → +5% cap) replacing the pre-ADR-0009 version; `resolveEffective` "has no deload parameter at all" → the `holdProgression` flag (the exact IB-050 drift, now also purged here); the dates line's "duplicated across views, no shared util" → `js/dates.js` is the shared util (never re-implement locally); and "no CI runs / always deploy manually" → the auto-deploy pipeline with its `CACHE`-bump guard. Docs-only, no trip-wire | audit-loop L2 |
| IB-049 | ADR-0002/0003/0004 carry no `[ADR-000N]`-tagged test, so lens L3 re-flags them every pass. The behaviour *is* covered (21 deload/cadence cases incl. `Deload week (non-retest) … cuts prescribedSets ~40%` and `[Phase2 C2] no two adjacent weeks are both deload…`) — the fix is tagging the cases that already exist | eng | P3 | — | Closed (audit-loop pass 9, 2026-08-01) — tagged the cases that already pin each Decision: `[ADR-0002]` on the eight `buildPhasePattern`/`resolveDate`/`cycleWeeksOf` derivation cases (`04-program-core.js`), one of them also `[ADR-0004]` since it already asserts the wk4/not-wk3 3:1 cadence; `[ADR-0003]` on the two deload-volume-cut / retest-exempt cases; `[ADR-0004]` on `[Phase2 C2]` (`10-phase-regressions.js`). L3 now prints empty. Test-name-only diff (11 relabels, zero logic), suite **245/0** | audit-loop L3 |
| IB-053 | `densityRest`'s base rest is passed explicitly at all three call sites (`densityRest(weeksLeft, 240)` — `program.js:495` inside `buildThuMain`, and `:618`/`:649` inside `buildSatMain`) even though `240` is the parameter's own default, so after IB-033 named it the number still appears in four places. No caller ever varies it, so the parameter is speculative generality; the `domain-invariants` skill already describes the intended interface as `densityRest(weeksLeft)` — one argument. Fix = drop the argument (and probably the parameter). **Not inline-buildable:** all three call sites are lines *inside* `buildThuMain`/`buildSatMain`, i.e. trip-wire 1, so it needs the multi-session path even though it is provably behaviour-neutral | eng | P3 | — | Closed (audit-loop pass 20, 2026-08-02, **user-authorized trip-wire build**) — verified `DENSITY_BASE_REST_SEC === 240`, so `densityRest(weeksLeft, 240) === densityRest(weeksLeft)`; dropped the `240` argument at all three sites (`:495`/`:618`/`:649`) **and** the now-speculative `baseSec` parameter (no caller varied it), using the named constant in the body. Aligns the code with the `densityRest(weeksLeft)` interface the `domain-invariants` skill already documents. Behaviour-neutral, proven both ways: **regenerated schedule byte-identical** + suite **248/0** (density behaviour already pinned by the `[ADR-0006]` rest-tightening case, so no new tautological test). `sw.js`→v65 | audit-loop pass 7, split from IB-033 |
| IB-054 | `/audit-loop`'s coordination note asserts "Each session works its own designated branch, so commits never clobber" — that only holds if each session also has its own **checkout**, and here they share one working tree. Observed live in pass 7: a session working IB-028 committed onto pass 7's branch, then checked out its own branch in the shared tree, leaving pass 7's uncommitted edits stacked on the wrong branch alongside that session's deliberately-red TDD cases; a `git add -A` from either session would have swept the other's work into its commit, and the only reason it surfaced was a suite run jumping 237/0 → 238/1 between two invocations. The skill's stated mitigation (distinct `IB-###` per session) prevents *duplicate item choice*, not *tree interference* — a different failure mode. Fix: have a pass take a `git worktree` of its own, or at minimum re-check `git rev-parse --abbrev-ref HEAD` against its own branch before staging. **P2 basis:** this row is not audit-seeded, so the mechanical High/Medium/Low mapping does not apply; it is priced against `project-goals.md`'s order as the engineering analogue of a G3 durability bug — it can silently destroy committed-nowhere work — and matched to **IB-051**, the other skill-defect row, which was also P2. Noted with it: a P2 here delays lenses L4–L6, whose trigger is fewer than five open P1/P2 | eng | P2 | — | Closed (audit-loop pass 8, 2026-07-30) — stage **1** became "Isolate and survey" and opens a detached `git worktree` before anything else, since L1 regenerates artifacts and would otherwise dirty the shared tree; stage 3 just names the branch on it. The false premise is replaced by the two collision modes stated separately, since a distinct `IB-###` addresses only the first | audit-loop pass 7 |
| IB-055 | Parallel passes can bump the `sw.js` CACHE counter to the **same** value, blocking a deploy. `generate-sw.mjs --bump` increments from whatever the working tree has, so two sessions branching off one `main` both produce `v57 → v58`; the merge is clean because both sides made the *identical* edit, and the second payload then carries a version the first already published — the two `sw.js` files are byte-identical (same blob `9e1011d`), so a browser would see no change and never reinstall. **The deploy guard already catches this**, and it is the only thing that does: `.github/workflows/firebase-deploy.yml` fails the run when `js/`/`css/` changed but the CACHE line did not, which is exactly this shape. So the consequence is a **blocked deploy needing a manual re-bump, not a silently-stale client**. Observed 2026-07-30 — IB-033 (`js/program.js`) and IB-028 (`js/loads.js` + `js/views/today.js`) both produced `v58`; run `30520710339` on `8ac0936` **failed and did not deploy**, leaving production on the earlier payload until a `v59` bump unblocked it. Note **L1 is blind to it** by construction (it runs the generator *without* `--bump`, and SHELL is byte-identical), so the CI guard is the whole safety net. Residual value in fixing: derive `CACHE` from a content hash of the shell rather than a counter — idempotent and collision-free, retiring the "bump exactly once per pass" rule. Same family as **IB-054** (shared mutable state a branch does not isolate), but unlike IB-054 nothing is unguarded here. **Correction 2026-08-03 (pass 25, examined for an auto-merge build):** the row's claim that a content hash would also **retire the CI guard** is wrong, and the correction matters because acting on it would reintroduce a G3-adjacent stale-client risk. The guard fails when `js/`/`css/` changed but the CACHE line did not — and a content hash only updates *if the generator is re-run*. A developer who edits `js/` and forgets to regenerate `sw.js` produces exactly that shape (source changed, CACHE unchanged), so the guard is still the thing that catches them; content-hashing changes *how CACHE is derived*, not *whether someone regenerated*. So the real fix is **not** decision-free "less machinery": it must either keep a guard (better rephrased as a general "is `sw.js` regenerated?" check — which also catches SHELL drift, a strict superset of today's cache-only guard) or move regeneration into the deploy step itself. That is a deploy-pipeline design choice, so this is **build-gated on a small decision**, not a mechanical build — routed accordingly rather than auto-merged | eng | P3 | — | Closed (audit-loop pass 26, 2026-08-03, **user-authorized; decision delegated**) — **decision: keep the guard, switch CACHE to a content hash, drop `--bump`.** The key realisation that made it clean: the existing CI guard ("js/`css/` changed ⟹ CACHE line must change") **still works unchanged** under content-hashing — regenerate and the hash moves; forget and it doesn't, so the guard still fires on a forgotten regen. So no `.github/workflows/firebase-deploy.yml` change was needed, contradicting my own pass-25 worry that the guard had to be reworked. `generate-sw.mjs` now derives `CACHE = 'climb-planner-' + sha256(shell bytes).slice(0,12)` over every SHELL asset and drops the `--bump` branch. Verified: idempotent (re-run = no-op), deterministic (same hash across runs), and cache-busting (editing `js/app.js` moved `e84ed045b313`→`63891dc00d7c`, restore moved it back). Fixes the original collision (two branches editing different files now get *different* hashes, or a visible CACHE-line merge conflict — never a silent identical version) and retires the "bump exactly once per pass" rule. Also a bonus for **L1**, which IB-055 flagged as blind: running the generator now updates CACHE too, so L1's `git diff --quiet sw.js` catches a forgotten regen locally, not just SHELL drift. Updated every **live** `--bump` instruction (`CLAUDE.md`, `domain-invariants`/`audit-loop`/LENSES skills, `copilot-instructions.md`); dated historical records (ADR rollout notes, archives, pass logs) left as-is. Diff touches `sw.js` → ratchet 1 met by the green suite **254/0** (no `js` logic changed; the suite doesn't import `sw.js`, but ran it to satisfy the ratchet) | audit-loop pass 8, sharpened pass 25 |
| IB-056 | **Taper weeks have the same intensity-creep defect IB-028 just fixed for deload weeks, and it is reachable.** `applyTaperVolume` cuts a taper session's sets and appends "Taper — volume cut, intensity held" (ADR-0007), but nothing holds the ADR-0009 +2.5% targets-hit step there: `buildPhasePattern` sets `deload:false, retest:false` on taper weeks, so `Loads.holdProgressionFor` returns null. The taper Monday **does** carry a kg-bearing exercise (`mon-hangboard-taper` → "Near-max hangs (taper touch)", `loadPctRange`, 1 set) in all three focuses — so unlike the retest leg (unreachable, see the second [ADR-0009 addendum](adr/0009-intra-phase-progression.md)), this one bites. Two paths, verified 2026-07-30: **(a) trip/project** taper is 2 weeks and both Mondays share `sessionId` `mon-hangboard-taper`, so wk12 matches wk11 and a targets-hit wk11 earns +2.5% **on the final week before the goal date**; **(b) comp** taper is 1 week with no in-cycle match, but `resolveForDay` scans every logged day regardless of cycle, so a *previous cycle's* taper Monday matches. Directionally identical to IB-028 — a week whose own note says "intensity held" ratcheting intensity up — and it lands where freshness matters most (ADR-0007 taper exists to arrive fresh). **Not fixed with IB-028 deliberately:** #64 scoped taper out because no decision covers it, and extending a load-chain trigger to a new phase is a training-content call, not a refactor. Needs a `/grill-queue` decision (is a taper-week hold correct, or does the taper *want* its intensity touch to creep?), then the same one-expression fix. Found by the `/code-review` Standards axis on #64. **Enqueued for grilling 2026-08-02 (audit-loop pass 10)** — [grill-queue #68](https://github.com/slm37102/simple-climbing-training-planner/issues/68), row added to [grill-queue.md](grill-queue.md). Re-verified against current code that pass: `holdProgressionFor` returns null on taper (`js/loads.js:148`), `resolveEffective` then applies +2.5% (`:194`/`:198`); taper Monday is `mon-hangboard-taper` via `` sessionId: `mon-hangboard-${phase}` `` (`js/program.js:362`) carrying `loadPctRange [0.90, 0.94]`; both taper Mondays share that id. Stays **Open** — enqueuing routes the decision; the ADR + `KG-*` + build are still ahead | eng | P2 | G3 | Open | audit-loop / #64 review |
| IB-057 | `Program.resolveDate` reimplements `daysBetween` inline — `const diffDays = Math.floor((d − start) / 86400000)` (`program.js:1212`), the same helper-copy IB-047 removed from the views, but in the **core** resolver (`program.js` already imports `snapToMonday`/`addDays` from `dates.js`). Behaviour-identical to `daysBetween(startDateStr, dateStr)` (both floor, both `T00:00:00`-parsed). Split from IB-047, whose scope is the views; `resolveDate` sits in the training core, so although it is neither a session builder nor a `PRESCRIPTION_PASSES` pass nor a %/RPE constant (i.e. not obviously trip-wire 1), a `program.js` date-math edit should be made deliberately and proven byte-identical via `node tools/generate-schedule.mjs` (schedule unchanged) + a green suite. Provably behaviour-neutral, so inline-buildable with that proof | eng | P3 | — | Closed (audit-loop pass 13, 2026-08-02) — replaced `Math.floor((d − start)/86400000)` with `daysBetween(startDateStr, dateStr)` and dropped the now-dead `const start = new Date(...)` parse (`d` stays — `resolveDate` still needs `d.getDay()` for the slot). Confirmed `start` had no other use in the function. Not trip-wire 1 (the resolver is not a session builder / pass / %-RPE constant). Behaviour-neutrality proven the two ways the row asked for: **regenerated schedule byte-identical** + suite **246/0** (resolveDate is already densely pinned by the ADR-0002 cycle-length cases, so no new tautological case added). `sw.js`→v62. Completes the `daysBetween` migration begun in IB-047 | audit-loop pass 12, split from IB-047 |
| IB-058 | **No readiness check-in silently down-regulates every session to "Lighter."** `today.js:493` substitutes `{sleep:3,soreness:3,fatigue:3}` when a day has no check-in; that averages 3.0 and `Loads.computeReadinessMultiplier` scores `avg ≥ 2.5 && < 3.5` as **Lighter (×0.85)** (Normal needs `≥ 3.5`), so the readiness-gate pass fires — climbing `prescribedTarget` ×0.85 + RPE cap ≤8.5 — and the kg suggestion is ×0.85. But `computeReadinessMultiplier(null)` returns **Normal**, so the view defeats the function's own "no data → Normal" contract; the root cause is a scale-midpoint (3) / Normal-threshold (3.5) mismatch. `today.js:1077` (`getOrInitDay`) also **persists** `{3,3,3}` on save-without-check-in, freezing Lighter into the day record + the `readinessTrend` baseline. Broad prescription impact (every un-checked-in session trains light → under-dosing, G1/G2) with a G3-conservative counter-argument, so it's a decision, not a unilateral fix. **Enqueued for grilling 2026-08-02 (audit-loop pass 17)** — [grill-queue #70](https://github.com/slm37102/simple-climbing-training-planner/issues/70), row in [grill-queue.md](grill-queue.md). Stays **Open** | eng | P2 | G1 | Open | audit-loop pass 17 survey |
| IB-059 | **Append-only retest `history` is lost across sync.** `Storage.saveRetestBenchmarks` pushes a dated snapshot onto `globalBenchmarks.history` and treats it as append-only ("never overwritten", ADR-0014, v6), feeding the retest-trajectory monitoring signal. But `mergeRemote` (`storage.js:534-535`) merges `globalBenchmarks` by **wholesale LWW replace** (`state.globalBenchmarks = remote.globalBenchmarks` when remote's `updatedAt` is newer), so the entire `history` array is overwritten, not unioned. Two devices that each log a retest before a sync round-trip → the older-`updatedAt` device's snapshot is silently dropped (verified via the LWW trace: A@T1, B@T2>T1 → remote ends B, A's onSnapshot takes B wholesale, entry A lost). The scalar benchmark values LWW correctly; only the append-only array loses entries. **Narrow** (single-user app, retests ~6-weekly, needs near-concurrent multi-device retests) but a real data-integrity gap — the append-only guarantee holds locally, not across sync. Fix = union-merge `history` by `date` (dedupe) instead of wholesale replace; needs no decision but touches `mergeRemote` (**trip-wire 3**), so multi-session `/implement`. Found by L4 (G3/data-integrity lens) | eng | P3 | G3 | Closed (audit-loop pass 21, 2026-08-02, **user-authorized trip-wire build**) — added a `mergeBenchmarkHistory(a, b)` helper (union by date, same-date dedupe → remote wins, sorted ascending) and rewrote `mergeRemote`'s globalBenchmarks branch to LWW the scalar fields **but union `history`** regardless of which side's `updatedAt` is newer, so no snapshot is dropped. Renamed the stale `[ADR-0014]` "LWW on the whole object" test to "scalars LWW, history unioned" (still passes — its remote was a superset) and added `[IB-059]` pinning the exact loss the old code caused: a **newer** remote that lacks a local-only snapshot now preserves it (`['2026-05-01','2026-06-08','2026-06-15']`), with a scalar-LWW check and an idempotency (no-sync-loop) assertion. Suite **249/0** (would fail on the old wholesale replace — got length 2). `sw.js`→v66 | audit-loop pass 19 (L4) |
| IB-061 | **The pre-v5 schema-migration chain had no round-trip test** — only v5→v6 was pinned (`[ADR-0014]`, `15-monitoring.js`). The higher-stakes earlier steps were bare, above all the **v3→v4 structural wrap** that reshapes a legacy flat `{settings,benchmarks,days}` into `{plans,activePlanId,globalSettings}` — the path a returning user who last opened the app pre-multi-plan hits on load, and where a regression would silently drop their logged `days` (their whole training history). `importJson` can't reach it (it throws unless `plans` already exists, `storage.js:471`), so `migrate` runs only via the LocalStorage load path (`Storage.init`) | eng | P3 | G3 | Closed (audit-loop pass 27, 2026-08-05) — added two `[IB-061]` characterization cases to `tests/cases/01-inputs-storage.js` driving the real load door (`localStorage.setItem('climb-planner:state', legacyV3)` → `Storage.init()`, since `importJson` rejects a plan-less state): (1) a **v3→v6 round-trip** asserting the chain reaches v6, the flat top-level keys are removed, **logged days + actuals survive** (the data-loss guard), user settings (cycleWeeks/peakType/units) carry into the plan, benchmark scalars promote to `globalBenchmarks`, and the legacy per-plan `benchmarks.history` is preserved; (2) **idempotency** — a second `init()` on the migrated state neither duplicates the plan nor loses data. Behaviour captured from a Node `localStorage`-shim probe first, so the test pins **real current behaviour**, not an assumption. **Observation, recorded not baked in as correct:** the legacy per-plan `benchmarks.history` is **not** promoted into `globalBenchmarks.history` (stays `[]`) at v4→v5, so a returning user's old retests would be invisible to the ADR-0014 monitoring signal — but reachability hinges on whether any pre-v6 code ever populated that array, and a fix touches `migrate` (**trip-wire 4**), so the test asserts only the v6 `Array.isArray` invariant (never `=== []`) and this row records the gap for a future look. Scope is the v3→v6 structural chain; the v1→v2 benchmark-reset and v2→v3 legacy-`actual` string parse are left uncovered (lower stakes). Tests-only diff, no trip-wire; suite **256/0** (254 + 2) | audit-loop L6 |
| IB-060 | `onboarding.js` (the new-plan wizard) was the only view with **zero test coverage** — today/log/calendar/profile are each mounted somewhere in the suite, but `openOnboarding` never was, so a throw or an empty first screen (which would leave the athlete unable to create a plan) could regress silently | eng | P3 | — | Closed (audit-loop pass 23, 2026-08-03) — added `tests/cases/18-onboarding-view.js` (genuinely new area → new numbered file + `index.html` import) with two `[IB-060]` smoke tests that mount the wizard the way `app.js` does (`openOnboarding` appends its own `.wizard` overlay to `document.body`) and drive the shared render/wire loop: open → assert role=dialog + `1 / 5` + no Back; advance one step → `2 / 5` + discipline selector + Back appears; Cancel → overlay removed and `onDone` NOT fired. Covers open→step0→advance→cancel/teardown, not the full `create()` write path. Tests-only, no trip-wire; suite **252/0** | audit-loop L6 |
| IB-064 | **Readiness "Push" (×1.05) raises finger/pull kg on recovery/hold weeks, bypassing `holdProgression`.** `holdProgressionFor` suppresses only the ADR-0009 **+2.5% targets-hit step** (`loads.js:194`), but the readiness multiplier — including `1.05` "Push" (`loads.js:28`) — is applied **unconditionally afterward** (`loads.js:210-213`, `kg *= readinessMultiplier`) and gated by nothing. The +5% cap is `prev × decay × 1.05` (`:220`), so the Push lands in full. Net: on a deload/retest/taper/**pain-amber** week a good-readiness check-in raises finger/pull load **+5%** — *larger* than the +2.5% `holdProgression` exists to stop, on the highest-injury-risk tissue exactly when least wanted (Push is likeliest when the athlete is freshest = a deload week; pain-amber means the athlete *reported finger pain* that day, yet still gets +5%). Verified trace against current code (12-wk comp Base deload Monday `BASE_MAX_INTRO`, prev 20kg targets-hit, readiness Push → 21kg; +2.5% suppressed, ×1.05 applied, cap = 21 so no clamp). **Distinct from every known row:** IB-028/056 concern the +2.5% step only (the *other* upward lever); IB-030/058 are the *downward* ×0.85 on *climbing volume* (opposite direction, different axis); IB-025 is a training argument for a finger deload; IB-029 questions the ×1.05 *magnitude* — this is about *when* it applies, and stands regardless of the number. **Upstream note for IB-056:** adding a `taper` branch to `holdProgressionFor` will NOT close this — the real fix is a hold also clamping `readinessMultiplier ≤ 1.0` (or flooring the move at prev actual) when `holdProgression` is set. A `resolveEffective` change (**trip-wire 2**) + a training call ("is a Push bump on fingers ever wanted during recovery?"), so decision-gated → grill queue. **Enqueued for grilling 2026-08-06 (audit-loop pass 31)** — [grill-queue #76](https://github.com/slm37102/simple-climbing-training-planner/issues/76), row in [grill-queue.md](grill-queue.md). Confirmed still-open against the **IB-056 decision** (2026-08-05, ADR-0007 addendum / KG-B17): that grill scoped its fix to "suppress *only* the +2.5% step" and kept the readiness lever running, so this facet is deferred to here, not covered there. Stays **Open** — enqueuing routes the decision | eng | P1 | G3 | Open | audit-loop L4 (pass 31 survey) |
| IB-062 | `Storage.duplicatePlan` — the deep-clone behind the Profile "Duplicate" button (`profile.js`) — had **zero test coverage** (`grep duplicatePlan tests/` → none). It must produce a deep, independent clone with a fresh id, `name` defaulting to `src.name + ' (copy)'`, and **empty `days`** (`clone.days = {}`, `storage.js:342`): the copy reuses the schedule/benchmarks but starts with no logged sessions. A dropped `clone.days = {}` would silently inherit the source's entire training log into the copy; a shallow clone (`{...src}` in place of the `JSON.parse(JSON.stringify)` at `:338`) would alias source state so logging into one plan corrupts the other. Both are realistic edits that pass the suite today. Tests-only, no trip-wire | eng | P3 | G3 | Closed (audit-loop pass 32, 2026-08-06) — added `[IB-062]` to `tests/cases/01-inputs-storage.js`: `addPlan` a `focus:'boulder'` source with a logged day, `duplicatePlan` it (no name → default), then assert a fresh id, the `'Base (copy)'` default name, carried-over `focus`, **zero `days`**, and deep independence — logging a day into the copy leaves the source's `days` untouched and the source's own logged day intact. **Red-checked** (pass-7 discipline): dropping `clone.days = {}` fails exactly this case and only it (257/1), so it's non-tautological. Tests-only, no trip-wire; suite **258/0** | audit-loop L6 (pass 31 survey) |
| IB-063 | The **v2→v3 legacy-`actual` string parse** (`parseLegacyActual`, `storage.js:94-104`) is untested — IB-061 covered the v3→v6 chain and **explicitly left this step uncovered** ("the v1→v2 benchmark-reset and v2→v3 legacy-`actual` string parse are left uncovered"). Loading a pre-v3 state whose `exercise.actual` is a legacy string like `"5x2 @ 62kg RPE 9"` must migrate it into structured `{raw, sets:5, reps:2, kg:62, rpe:9}` via three regexes; a regression in any regex silently blanks a returning user's historical logged results on the one-time upgrade (data loss). Only reachable via the LocalStorage load path (`Storage.init`), not `importJson` (which throws without `plans`). Tests-only, no trip-wire | eng | P3 | G3 | Closed (audit-loop pass 33, 2026-08-06) — added `[IB-063]` to `tests/cases/01-inputs-storage.js`: seed a `version:2` state whose `actual` is `"5x2 @ 62kg RPE 9"`, `Storage.init()` (runs migrate v2→…→v6), assert the day survives and the string parses to `{sets:5, reps:2, kg:62, rpe:9, raw}`. **Red-checked** — nulling the `@…kg` regex fails exactly this case (258/1). Completes the migration coverage IB-061 began (v3→v6) by pinning the v2→v3 step it scoped out. Tests-only, no trip-wire; suite **259/0** | audit-loop L6 (pass 31 survey); IB-061 scope-out |
| IB-065 | `Storage.deletePlan` (`storage.js:324-330`) has **zero coverage** (`grep deletePlan tests/` → none): it must throw `'Cannot delete the only plan.'` when one plan remains, and reassign `activePlanId` to a surviving plan when the active plan is the one deleted. A dropped guard (deleting the last plan → a plan-less state that `get()`/`getActivePlan` later throw on) or a missed reassignment (a dangling `activePlanId`) would pass silently. Minor — smaller blast radius than IB-062 — but a live path. Tests-only, no trip-wire | eng | P3 | — | Closed (audit-loop pass 34, 2026-08-06) — added `[IB-065]` to `tests/cases/01-inputs-storage.js`: `addPlan` a second plan, `deletePlan` the **active** one → assert it's gone and `activePlanId` moved to the survivor; then `deletePlan` the last → assert it throws `'Cannot delete the only plan.'` and the plan is not deleted. **Red-checked** — disabling the reassignment line fails exactly this case (259/1). Tests-only, no trip-wire; suite **260/0** | audit-loop L6 (pass 31 survey) |
| IB-066 | `Warmup.forSession`'s warm-up **content** routing is unpinned — `tests/cases/08-drills.js` builds real sessions but asserts only `.skillDrills`, never `.warmup`. The `mon-hangboard*`/`mon-retest` branch appends `HANGBOARD_PROGRESSION` (the progressive-load finger warm-up before max hangs, `warmup.js:56`) and the `thu-`/`sat-` branch appends `CLIMBING_PROGRESSION` (`:57`), keyed on `session.sessionId` prefixes emitted by `program.js`. The hangboard branch — the app's key pulley/tendon injury guard (progressive loading before maximal 20mm hangs) — is completely unprotected: rename `mon-hangboard-*` in a `program.js` refactor and the finger warm-up silently vanishes with no failing test. Tests-only, no trip-wire | eng | P3 | G3 | Closed (audit-loop pass 35, 2026-08-06) — added `[IB-066]` to `tests/cases/08-drills.js`: derive a real Mon-wk1 (`mon-hangboard-base`) and Thu-wk1 (`thu-projecting-base`) session from `Program.prescribeForContext`, then assert the Monday warm-up carries the progressive finger hangs (and *not* the climbing progression), the Thursday warm-up carries the climbing progression (and *not* the hangs), and a rest session carries no warm-up. Derives from `Program` (not a stub), so it pins the `program.js` sessionId ↔ `warmup.js` prefix-routing coupling. **Red-checked** — dropping the `mon-hangboard` prefix from `isHangboard` fails exactly this case (260/1). Tests-only, no trip-wire; suite **261/0** | audit-loop L6 (pass 35 survey) |
| IB-067 | The first-run **empty-state** renders are never mounted in a test — `today.js:456` ("Set up your cycle") and `calendar.js:24` ("No cycle configured") render when `Program.effectiveStart` is null, but every today/calendar case sets `startDate` first, so neither `!start` branch is ever exercised. A refactor moving grid-building above the null guard would crash the very first screen a new athlete sees (unable to onboard) with no failing test. Tests-only, no trip-wire | eng | P3 | — | Closed (audit-loop pass 36, 2026-08-06) — added `[IB-067]` to `tests/cases/02-today-view.js`: `resetStorage()` (fresh plan, `startDate`/`compDate` null → `effectiveStart` null), then `renderToday` asserts the "Set up your cycle" card + `#profile` link, and `renderCalendar` asserts the "No cycle configured" card. **Red-checked** — bypassing the Today `if (!start)` guard fails exactly this case (261/1). Tests-only, no trip-wire; suite **262/0** | audit-loop L6 (pass 35 survey) |

## Pass log

One line per `/audit-loop` pass — what the lenses found, what closed, where it shipped. Keeps the loop resumable and auditable.

- **2026-07-29** — pass 0 (bootstrap): brought `deep-audit.md` onto `main` (`1f836ab`, docs-only); seeded IB-001…IB-046 from its 46 findings (IB-007/IB-023 already Closed by ADR-0016); carried `improvement-audit.md`'s partial Q1 across as IB-047; L2 raised IB-048; L3 raised IB-049. L1 clean — generated artifacts in sync. 49 rows, 47 open — 3 P1 · 16 P2 · 28 P3, split 18 `eng` · 29 `training`.
- **2026-07-29** — pass 1: L1 clean · L2 raised **IB-050** · L3 nothing new (IB-049 already owns it). L4–L6 parked (19 open P1/P2, threshold is <5, no `--full`). Frontier IB-028 re-verified against current code and **routed to the multi-session path** on trip-wire 6, un-actioned here; its row now records that `holdProgression` already exists so the fix is one expression in `today.js`. Closed **IB-050** — the `CLAUDE.md` drift that hid exactly that. Also filed **IB-051** (this skill presumes handoffs it cannot make). No GitHub issue minted — the work was docs-only and done inline, so there was no `/implement` handoff for a ticket to serve; that gap is part of IB-051. Docs-only, so ratchet 1 satisfied trivially; auto-merged. 51 rows, 48 open — 3 P1 · 17 P2 · 28 P3, split 19 `eng` · 29 `training`.
- **2026-07-29** — pass 2: L1 clean (sw.js + schedule regenerated, no drift, tree restored) · L2 spot-checked the load-chain/pipeline invariants against code — `SCHEMA_VERSION` 6, `DOUBLE_BLOCK_THRESHOLD` 20, `DELOAD_VOLUME_MULTIPLIER` 0.6, and the seven-pass `PRESCRIPTION_PASSES` order all still literally true; no new drift beyond the already-filed IB-048 · L3 nothing new (IB-049 owns 0002/0003/0004). L4–L6 parked (20 open P1/P2, threshold is <5, no `--full`). Frontier is still **IB-028**, blocked on the un-invokable multi-session handoff — which is exactly **IB-051**, so this pass closed **IB-051** to unblock the loop: confirmed all five delegated skills (`/implement`, `/grill-with-docs`, `/to-spec`, `/to-tickets`, `/improve-codebase-architecture`) are `disable-model-invocation: true`, then rewrote the skill's intro + stages 3/4 to acknowledge the agent-invocation constraint and make issue-minting conditional on a real handoff. Skill-file-only diff, no trip-wire, no `js/`/`css/`/`sw.js`/`tests/` touched, so ratchet 1 satisfied trivially; no GitHub issue minted (inline pass, no downstream consumer — per IB-051's own fix). Pushed to `claude/audit-loop-8uuw7y` per the session's branch directive. 51 rows, 47 open — 3 P1 · 16 P2 · 28 P3, split 18 `eng` · 29 `training`.
- **2026-07-29** — pass 3: L1 clean (regenerated, no drift, restored) · L2 walked ADR-0002's claims against `js/program.js` and confirmed the IB-003 drift · L3 nothing new (IB-049 owns 0002/0003/0004). L4–L6 parked (19 open P1/P2, threshold <5, no `--full`). Frontier **IB-028** remains a trip-wire multi-session item an agent cannot invoke the handoff for — reported the `/grill-with-docs` command to the user, un-actioned here. Drained the highest-priority *inline-buildable* item instead: closed **IB-003** via a dated **ADR-0002 addendum** (ratchet 4) recording that ADR-0004 moved deload cadence to `(i+1)%4` and ADR-0007 moved taper length to `taperWeeksFor(peakType)` + the `buildPhasePattern(weeks, peakType)` signature. Docs-only (ADR + ledger), no trip-wire, no `js/`/`css/`/`sw.js`/`tests/` touched → ratchet 1 trivial; no GitHub issue minted (inline pass). Pushed to `claude/audit-loop-8uuw7y`. 51 rows, 46 open — 3 P1 · 15 P2 · 28 P3, split 17 `eng` · 29 `training`.
- **2026-07-29** — pass 5: L1 clean · L2/L3 nothing new. L4–L6 parked (17 open P1/P2). Frontier **IB-028** still trip-wired/handed back. Closed **IB-041**'s sibling comprehension gap **IB-042** — the Today "flavor" badge showed bare "boulder"/"sport"/"hybrid" with no tooltip (the adjacent energy badge has one). Added `flavorLabel()` + a `title` on the badge in `js/views/today.js`, scoped so the alternation caveat only claims what's true on a hybrid plan; no `program.js` edit. **Split out the deeper defect as new row IB-052** (the badge *value* can contradict the prescribed session on hybrid Build — a `program.js`/trip-wire concern, so multi-session) rather than silently leave IB-042 half-done. Suite headless → **230 passed, 0 failed** (+ a new `[IB-042]` case). `/code-review` both axes clean (Spec explicitly OK'd the partial-close-plus-split as honest). Regenerated `sw.js` (`--bump` → v56). Touches `js/`+`tests/`+`sw.js`; ratchet 1 met by a **green** suite. No issue minted (inline). Pushed to `claude/audit-loop-8uuw7y`. 53 rows, 45 open — 3 P1 · 13 P2 · 29 P3, split 16 `eng` · 29 `training`.
- **2026-07-29** — pass 4: L1 clean · L2/L3 nothing new (also confirmed the suite is exactly 228 cases, matching LENSES.md's figure — no drift there). L4–L6 parked (18 open P1/P2). Frontier **IB-028** still trip-wired/handed back. Built the highest-priority *inline* eng item: closed **IB-041** — `Loads.resolveForDay`'s `reason[]` was computed but never rendered (the "UI tooltip" the loads.js/CLAUDE.md comments promised didn't exist). Added a `loadReasonBadge()` + shared `infoBadge()` to `js/views/today.js`, rendering the load-chain provenance as the same info-badge+`title` idiom the energy badge uses; no `loads.js` edit (that file is trip-wire 2, even for a comment). Ran the browser suite headlessly (global playwright + Chromium, tiny static server) → **229 passed, 0 failed** (228 + a new `[IB-041]` regression case through the `renderToday` seam). Ran `/code-review`: Standards flagged the info-badge dedup (fixed via the shared `infoBadge()`); Spec caught the badge leaking onto the rest-suggested branch where `reason[]` is uninformative (fixed — badge now kg-only). Regenerated `sw.js` (`--bump` → v55). Touches `js/`+`tests/`+`sw.js`; ratchet 1 satisfied by a **green** suite. No GitHub issue minted (single-session inline build). Pushed to `claude/audit-loop-8uuw7y`. 52 rows, 45 open — 3 P1 · 14 P2 · 28 P3, split 16 `eng` · 29 `training`.
- **2026-07-30** — pass 6: L1 clean (regenerated, no drift, tree restored) · **L2** walked the `holdProgression` chain — both `domain-invariants` claims are still *literally* true of the code (the flag is amber-pain-only until IB-028 ships), so no new row; instead recorded updating those two bullets as **IB-028 build scope**, since IB-050 already had to close that exact drift once. Also confirmed this pass's own refactor introduced no invariant drift (neither invariant file nor `copilot-instructions.md` names the moved code) · L3 nothing new (IB-049 owns 0002/0003/0004). L4–L6 parked (16 open P1/P2, threshold <5, no `--full`). Frontier **IB-028** left un-actioned for a *new* reason: its decision landed 2026-07-30, so it is no longer decision-gated, but the ADR-0009 addendum's own "Build (separate, multi-session) … not inline" clause routes it to `/to-spec → /to-tickets → PR`, and ratchet 4 says that stands — handed back, and deliberately did **not** mint its build ticket (the skill assigns that to the build path). Frontier bullet rewritten to say build-gated rather than decision-gated. Drained the highest-priority *inline-buildable* item instead: closed **IB-032** — moved the ADR-0012 micro-retest staleness rule to `Warmup.isMicroRetestDue({ctx, benchmarks, dateISO})` (beside the threshold and the warm-up step it gates) and the readiness key→gate-label adapter to `Program.readinessGateLabel(key)`. The audit rated that second half "cosmetic"; it is not — `Loads` mints `'rest'` while the readiness-gate pass matches `'suggestRest'`, so the view was the *only* thing holding two vocabularies together, with nothing pinning them. `js/loads.js` untouched (trip-wire 2), and no `program.js` session builder / pass / %-RPE constant touched. Three signals the extraction was faithful: `daysBetween`+`localIso` fell out of `today.js` as dead imports, the regenerated schedule was byte-identical, and a new equivalence case pins the ctx-derived phase pattern against the settings-derived one across 4 cycle-length/peakType combos. `/code-review`: **Standards** no hard violations (no import cycle; `warmup.js`→`program.js` verified acyclic) + 3 judgement calls — acted on the sharpest by dropping the redundant `settings` param (ctx already carries clamped `cycleWeeks` + normalized `peakType`) and importing named functions instead of the `Program` singleton; kept the rule in `warmup.js` because the audit's Recommendation names that file. **Spec** flagged the moved applicability gate as mild scope creep (accepted — it buys one named predicate), OK'd `program.js` as the adapter's home given `loads.js` is trip-wired, and caught that a null `ctx` used to *throw* — kept the safer `false`, but relabelled the test so it pins the hardening honestly instead of claiming preserved behaviour. Suite **235 passed, 0 failed** (230 baseline + 5 `[IB-032]` cases). `sw.js` → v57. Touches `js/`+`tests/`+`sw.js`; ratchet 1 met by a **green** suite. No issue minted (inline build, no downstream consumer). 52 rows, 44 open — 3 P1 · 12 P2 · 29 P3, split 15 `eng` · 29 `training`.
- **2026-07-30** — pass 7: L1 clean (regenerated, no drift, tree restored) · **L2** walked ADR-0002's phase-split claim and ADR-0006's density claim against code — both still literally true, and this pass introduces no new drift: the constants it names are module-private, exactly like the `DOUBLE_BLOCK_THRESHOLD`/`DELOAD_VOLUME_MULTIPLIER` the invariants file already cites, and the documented `densityRest(weeksLeft)` interface is unchanged · L3 nothing new (still 0002/0003/0004 — IB-049 owns it). L4–L6 parked (15 open P1/P2, threshold <5, no `--full`). Frontier **IB-028** left alone for a third consecutive pass, now for the best possible reason: it is `Spec'd` with build tickets minted and a **parallel session is actively building it**. IB-014/IB-024 remain grill-queue decisions. Drained the highest-priority inline-buildable item: closed **IB-033** — named `BUILD_FRACTION_OF_REMAINING` (inlined at three phase-derivation sites: `_singleBlock` ×1, `_doubleBlock` ×2) and the `DENSITY_*` cluster, collapsing an unnamed `4` and `3` that were the *same* window length on adjacent lines — the one place in the cluster where drift could actually mis-prescribe. Re-verification also **killed one of the audit's three sub-claims**: `DOUBLE_BLOCK_THRESHOLD` was already a named constant with a rationale comment, so that third of the finding was wrong at import time — worth recording, since this row carried the audit's own *unverified* caveat. **Split out IB-053** for the honest remainder (the redundant `densityRest(weeksLeft, 240)` argument sits inside session builders = trip-wire 1). Behaviour-neutrality has three independent signals: regenerated schedule **byte-identical**, `sw.js` SHELL unchanged (diff is the single CACHE line, → v58), suite green. Two new `[IB-033]` cases pin what naming a constant invites someone to retune — the `DOUBLE_BLOCK_THRESHOLD` boundary (20 wk single / 21 wk double; previously only 24 wk was covered, so `>` vs `>=` was unpinned) and exact Base/Build counts either side of it. A first draft of that second case asserted a Base ≥ 2×Build ratio and was simply **wrong** — it fails at 25/31/37 wk, where `max(2, round(…))` pushes the realised build share to 0.35–0.36 — so it was replaced with exact-count pinning instead of shipped. Suite **237 passed, 0 failed** (235 baseline + 2). No issue minted (inline build, no downstream consumer). 54 rows, 45 open — 3 P1 · 12 P2 · 30 P3, split 16 `eng` · 29 `training`.
  - **Cross-session collision, recorded because it nearly cost work.** Mid-build, the parallel IB-028 session committed onto *this pass's branch* and then switched the **shared working tree** to `claude/ib-028-1-holdprogression-cause`, leaving this pass's uncommitted edits stacked on their branch beside their deliberately-red TDD cases. It surfaced only because a re-run of the suite jumped from `237 passed, 0 failed` to `238 passed, 1 failed` between two invocations — the failure being *their* red test, not this pass's. Recovered without touching their work: saved the three files as a patch, re-based this branch on `main` inside a separate **`git worktree`**, re-ran the suite there in isolation (same 237/0, with no IB-028 cases present — confirming clean separation), then reverted the three files in the shared tree so their checkout was clean again. Their commit stays reachable from their own branch. Filed as **IB-054**: the skill's coordination note covers a shared *ledger* but assumes a checkout per session, which is not the case here.
  - **`/code-review`.** **Standards:** no hard violations; both factual claims in the new comments independently re-derived (`round(r×0.33) === round(r/3)` for all reachable `remaining`; the window bottoms out at 3:40 so the floor never binds), and the `> 3` → `>= DENSITY_WINDOW_WEEKS` rewrite confirmed exactly equivalent for every reachable `weeksLeft` — it rests on integrality, not algebra, which is why the precondition is now a comment. Acted on two of its judgement calls: extracted `baseToBuildTransitions()`, which the new case had duplicated verbatim from the `buildPhasePattern(24)` case ten lines above, and renamed `BUILD_FRACTION` → **`BUILD_FRACTION_OF_REMAINING`** (it is a share of `remaining`, not of the cycle — and on an item whose whole point is naming, the name should carry it). **Spec:** confirmed the retired sub-claim and the honest split, and caught a real defect — IB-053 cited *pre-diff* line numbers, off by ~15 in the commit that shipped it (now `:495`/`:618`/`:649`, with enclosing builders named so the pointer degrades gracefully). Also added the trip-wire 1 disposal the row had left implicit: it argued only the "session builder" limb, never the "percentage/RPE constant" limb that is arguably readable onto a `0.33`. **One Spec finding rejected on the evidence:** it reported the `4`/`3` collapse as untested and `weeksLeft = 4 → null` as unpinned — but `[ADR-0006] Sat 4×4 rest tightens only inside the final 4 weeks` already pins both sides of that boundary (wk9/weeksLeft 3 → `3:55`; wk8/weeksLeft 4 → the `|| '4 min'` fallback, which *is* the observable form of a `null` return). Post-review suite **237 passed, 0 failed**; schedule re-checked byte-identical after the rename. Shipped by merging to `main` (`5b04096`) and pushing — ratchet 1 met by the green suite, and the merge was taken **in the worktree** because the primary checkout was on the other session's branch, so `main` was free.
- **2026-07-30** — pass 8 (user-directed, skill-only): closed **IB-054** at the user's request, one pass after filing it. `/audit-loop` now takes a **checkout of its own**. First draft put that in stage 3 and was wrong: the lenses run in stage 1, and **L1 regenerates `sw.js` + the schedule**, so a stage-3 worktree arrives after the one step guaranteed to dirty a shared tree. Corrected before shipping — stage **1** is now "Isolate and survey" and opens `git worktree add --detach "$WT" main` (detached, because the branch name needs the `IB-###` the survey hasn't picked yet); stage 3 shrinks to `git -C "$WT" checkout -b claude/ib-0NN-<slug>`. The worktree must live outside the repo, or the generators and the primary tree's `git status` will see it. Four consequences threaded through: (1) the coordination note's false premise ("Each session works its own designated branch, so commits never clobber") is replaced by the **two collision modes stated separately** — same-item, which a distinct `IB-###` does fix, and same-checkout, which it does nothing for; (2) ratchet 5 widened from "leave the tree clean" to "leave *every* tree clean", naming the primary checkout and requiring the worktree be removed; (3) stage 4's git block now commits via `git -C "$WT"`, and handles the constraint that **`main` can be checked out in only one tree at a time** with an explicit two-case merge (primary tree if it holds main and is clean, else the worktree) — plus `gh pr create --head`, since `gh` otherwise opens a PR for whatever branch the primary checkout happens to be on; (4) `LENSES.md` re-pointed at `$WT`, because L1 regenerates artifacts and was the one lens guaranteed to dirty a shared tree. Also recorded two lessons pass 7 paid for: bump `sw.js` **once** per pass (a post-review `js/` change does not need a second bump), and on a behaviour-neutral refactor the byte-identical regenerated schedule *is* the proof, so state it as a check that can fail. Built in a worktree itself — dogfooding the fix; the primary checkout stayed on the other session's branch untouched throughout. Skill-and-ledger diff only: no `js/`/`css/`/`sw.js`/`tests/` touched, so ratchet 1 is satisfied trivially and no suite run was owed. No issue minted (inline, no downstream consumer).
  - **The push was rejected, which surfaced a cache-version collision.** *(Corrected later the same day: this bullet first called it a "live production bug" and filed IB-055 at P2. That was wrong — see the correction at the end.)* Between this pass's base and its push, the parallel session merged IB-028 part 1 to `main` (PR [#65](https://github.com/slm37102/simple-climbing-training-planner/pull/65)), touching `js/loads.js` + `js/views/today.js`. Reset `main` to `origin/main` and re-merged the feature branch — **clean, no conflict**, because their ledger edits (IB-028 row, grill queue) and this pass's (IB-054 row, pass log) fell in different regions. Then, checking whether the two passes actually *composed*, found that **both had bumped `sw.js` from `v57` to `v58`** off the same base: merged `main` claimed `v58` for a JS payload different from the `v58` this session had already deployed an hour earlier, so any client holding that first `v58` would never re-fetch their IB-028 fix. Bumped to **v59** on the merge so the combined payload gets a fresh key, and filed **IB-055** — with the sharper half of the finding being that **L1 is structurally blind to this**: it runs `generate-sw.mjs` *without* `--bump`, and SHELL was byte-identical in both, so its drift check exits 0 while the version is silently wrong. Ratchet 1 therefore *did* become owed (the diff now touches `sw.js`), and the suite was run on the merged tree rather than assumed from their PR: **241 passed, 0 failed** — this pass's 237 plus their four new `[IB-028]` cases, so the two passes compose. 55 rows, 45 open — 3 P1 · 12 P2 · 30 P3, split 16 `eng` · 29 `training`.
  - **Correction, same day — IB-055 was overstated and is now P3.** Asked whether it actually needed fixing, checked the deploy workflow instead of re-asserting, and found `.github/workflows/firebase-deploy.yml` already guards exactly this shape: it fails the run when `js/`/`css/` changed but the CACHE line did not. Run history confirms it fired — `5b04096` (IB-033) **success** → deployed `v58`; `8ac0936` (their PR #65) **failure** → deploy blocked; `60b6fb0` (`v59`) **success** → deployed both. So no client was ever served a poisoned cache key; `main` simply ran ahead of production for about an hour. The real consequence is a **loud blocked deploy needing a manual re-bump**, not silent staleness, which is a convenience item and not the G3-adjacent one the row first claimed. Re-priced **P2 → P3** and the row rewritten to lead with the guard. The half of the original finding that survives intact is that **L1 cannot see this** — it runs the generator without `--bump` and SHELL is byte-identical — so the CI guard is the entire safety net, which is worth knowing before anyone "simplifies" it away. Lesson for the loop: an audit finding that names a failure mode should check for an existing guard *before* pricing it, or it inflates its own priority. 55 rows, 45 open — 3 P1 · 11 P2 · 31 P3, split 16 `eng` · 29 `training`.
- **2026-08-01** — pass 9: L1 clean (regenerated `sw.js` + schedule, no drift, tree restored) · **L2** spot-checked the cadence/threshold/deload invariants adjacent to the item — `DOUBLE_BLOCK_THRESHOLD=20`, the `(i+1)%4` 3:1 cadence, `DELOAD_VOLUME_MULTIPLIER=0.6`, `BUILD_FRACTION_OF_REMAINING=0.33` all still literally true and the `domain-invariants` skill matches them (it already names IB-028/IB-056 correctly); no new drift · **L3** re-flagged 0002/0003/0004, which is exactly **IB-049**. L4–L6 parked (15 open P1/P2, threshold <5, no `--full`). All frontier P1s un-actionable inline: IB-028 was built via PR #66 (ticket path), IB-014/IB-024 are grill-queue decisions, IB-056 needs a `/grill-queue` decision. Drained the highest-priority *inline-buildable* item: closed **IB-049** by tagging the eleven existing cases that already pin ADR-0002/0003/0004's Decisions — `[ADR-0002]` on the eight `buildPhasePattern`/`resolveDate`/`cycleWeeksOf` derivation cases (`04-program-core.js`), `[ADR-0004]` added to the one that already asserts the wk4/not-wk3 3:1 cadence and to `[Phase2 C2]` (`10-phase-regressions.js`), `[ADR-0003]` on the two deload-volume-cut / retest-exempt cases. L3 now prints empty; the tag idiom matches the existing `[ADR-00NN]`/`[KG-B13]` convention. **Stale-base note:** local `main` was 40+ commits behind (`9d32c9f`); `origin/main` = the designated branch `claude/audit-loop-m5zmqf` = `3659aee` is the true production tip (the stage-1 `git log origin/main..main` check is empty when `main` is *behind*, so it didn't catch this). The stage-1 worktree had been opened off stale `main`, so it was discarded and L1/L3 + the edit re-run against the real tip in the primary checkout, which already held the designated branch clean — no parallel session was live (`git worktree list` showed only the primary), so the shared-tree collision IB-054 guards against did not apply. Test-name-only diff (11 relabels, zero logic): no `js`/`css`/`sw.js`/`program.js` touched → no `--bump` and no schedule regen owed; it touches `tests/`, so ratchet 1 met by a **green** suite — **245 passed, 0 failed** (headless Chromium + a static server). No issue minted (inline close, no downstream consumer). Per the session's branch directive, pushed to **`claude/audit-loop-m5zmqf`** rather than auto-merged to `main`. 56 rows, 45 open — 3 P1 · 12 P2 · 30 P3, split 16 `eng` · 29 `training`.
- **2026-08-02** — pass 10 (arg "take new task"): worktree opened at the designated branch tip (`1b94e9e`, carrying pass 9's unmerged IB-049) so that work is preserved and this pass stacks on it. L1 clean (regenerated `sw.js` + schedule in `$WT`, no drift, restored) · **L2** clean — walked the taper/`holdProgression` invariants against code and the `domain-invariants` skill (line 19) *already* describes the taper gap precisely and names **IB-056**, so no drift · **L3** now prints **empty**, confirming pass 9's IB-049 tags cover every ADR — the lens that re-flagged 0002/0003/0004 nine passes running is finally quiet. L4–L6 parked (14 open P1/P2, threshold <5, no `--full`). Frontier is **IB-056** (P2, G3) — the highest-priority open item after the two grill-queue P1s — a *trip-wire, decision-gated* item **not yet enqueued**, so per the skill this pass **enqueues it for grilling** rather than draining a lower P3 inline: minted [grill-queue #68](https://github.com/slm37102/simple-climbing-training-planner/issues/68) (label `grill-queue`, claim-by-comment protocol, no assignee), added a `Queued` row to `grill-queue.md`, and recorded the routing on the IB-056 backlog row. **Re-verified against current code before minting** (skill says re-verify at pick-up): taper weeks are `deload:false, retest:false` (`_composeSingle`/`_composeDouble`), so `holdProgressionFor` returns null (`loads.js:148`) and `resolveEffective` applies +2.5% (`:194`/`:198`); the taper Monday is `` `mon-hangboard-${phase}` `` → `mon-hangboard-taper` (`program.js:362`) carrying `loadPctRange [0.90, 0.94]`, and both trip/project taper Mondays share that id — the defect is real and reachable, matching the row. IB-014/IB-024 remain P1 grill-queue decisions; the P2 training items are decision-gated too but not yet phrasable as sharp questions (fog, per grill-queue.md). **Docs-only + one GitHub issue** — no `js`/`css`/`sw.js`/`tests/` touched, so ratchet 1 is satisfied trivially and no suite run or `--bump` was owed; L1's regen was for drift detection only and restored. Per the session's branch directive, pushed to **`claude/audit-loop-m5zmqf`** (stacked on IB-049), not auto-merged to `main`. Counts unchanged (IB-056 stays Open — enqueuing routes the decision, it doesn't close the row): **56 rows, 44 open — 2 P1 · 12 P2 · 30 P3, split 15 `eng` · 29 `training`** (this also corrects pass 9's line, which stated 3 P1 / 45 open — an off-by-one P1 miscount; no status changed between the passes).
- **2026-08-02** — pass 11: worktree at branch tip (`5733ad4`, stacking on passes 9–10). L1 clean (regen `sw.js` + schedule in `$WT`, no drift, restored) · **L2** was the productive lens — walked `.github/copilot-instructions.md` against code and found the drift **wider than IB-048's four named items** · **L3** empty (IB-049 tags hold). L4–L6 parked (14 open P1/P2). The P1s (IB-014/IB-024) and IB-056 are all grill-queue decisions; the P2 training items aren't yet phrasable as sharp questions; so the highest-priority *inline-buildable* item was the L2 doc-drift row **IB-048**. Closed it by fixing **all** the drift, each claim re-verified against current code: `buildPhasePattern(weeks, peakType)` + `taperWeeksFor` (ADR-0007); retest exempt Monday-only (`env.slot==='mon-main'`, KG-B10, `program.js:1069`); Log now read-only (removed test); `SCHEMA_VERSION` 5→6 + the missing v6 `saveRetestBenchmarks` history (ADR-0014); the full ADR-0009/0013/0014 load chain (layoffDecay → targets-hit +2.5% → `holdProgression` → readiness → +5% cap) with `holdProgressionFor`; `resolveEffective` "has no deload parameter" → the `holdProgression` flag (the same IB-050 drift, now purged from this file too); the dates line's "duplicated across views, no shared util" → `js/dates.js` is the shared util (never re-implement locally) — the exact opposite of what it said, and adjacent to open **IB-047**; and "no CI runs / always deploy manually" → the auto-deploy pipeline + its `CACHE`-bump guard. **Docs-only** — the diff touches only `.github/copilot-instructions.md` (not `js`/`css`/`sw.js`/`tests/`), so ratchet 1 is satisfied trivially and no suite run or `--bump` was owed; L1's regen was drift-detection only and restored. No issue minted (inline close, no downstream consumer). Per the branch directive, pushed to **`claude/audit-loop-m5zmqf`**. **56 rows, 43 open — 2 P1 · 12 P2 · 29 P3, split 14 `eng` · 29 `training`**.
- **2026-08-02** — pass 12: first *code* pass this session, so validated the headless suite runner (global Playwright at `/opt/node22` + pre-installed Chromium at `/opt/pw-browsers`, driven by a small Node script against a `python3 -m http.server` on the worktree — the Playwright **MCP** is pinned to a missing `chrome` channel and can't be used here). Baseline **245/0**. L1 clean · L2/L3 nothing new. L4–L6 parked (14 open P1/P2). Frontier P1s/IB-056 all grill-queue decisions; drained the top inline-buildable item **IB-047** (finish the `js/dates.js` migration). Swept all of `js/` for `/86400000` day-diffs: migrated the two genuine `daysBetween` copies **in the views** — `today.js:130` (exact floor) and `profile.js:395` (`Math.round`→floor, a comp-date hint, standardised on the app convention; ±1 only across a DST boundary in a future-dated hint). Left the calendar/onboarding/profile day-cursor loops (need the `Date` per cell, already use `localIso`/`mondayDow`) and the `new Date(iso+'T00:00:00')` **formatters** (CLAUDE.md keeps those view-local) — none is a helper copy. Added a named `[IB-047]` case pinning the floor/arg-order the migrated sites depend on (`daysBetween` semantics were already pinned by `[Phase4 Q1]`). **Spun off IB-057** for the same inline copy in `Program.resolveDate` (`program.js:1212`) — out of IB-047's view scope and in the training core, so filed rather than scope-crept. Touches `js/`+`tests/`+`sw.js`; ratchet 1 met by a **green** suite — **246 passed, 0 failed** (245 + the new case), `sw.js`→v61 (bumped once). No issue minted (inline close). Pushed to **`claude/audit-loop-m5zmqf`**. **57 rows, 43 open — 2 P1 · 12 P2 · 29 P3, split 14 `eng` · 29 `training`**.
- **2026-08-02** — pass 13: L1 clean · L2/L3 nothing new. L4–L6 parked (14 open P1/P2). Drained the item pass 12 spun off: closed **IB-057** — `Program.resolveDate` had the same inline `daysBetween` copy (`Math.floor((d − start)/86400000)`, `program.js:1212`), now `daysBetween(startDateStr, dateStr)` with the dead `start` parse removed (`d` kept for `d.getDay()`). A `program.js` edit, but not trip-wire 1 (the resolver is neither a session builder nor a `PRESCRIPTION_PASSES` pass nor a %/RPE constant), so inline-buildable — and proven behaviour-neutral the two ways its row demanded: **regenerated schedule byte-identical** and suite **246/0** (resolveDate is already densely pinned by the ADR-0002 cases; no tautological case added). Touches `js/`+`sw.js`; ratchet 1 met by the green suite, `sw.js`→v62 (bumped once). Together with IB-047 this retires every inline day-diff copy in `js/` outside the LWW timestamp comparator (`storage.js:558`, which is a datetime compare, not day-math). No issue minted (inline close). Pushed to **`claude/audit-loop-m5zmqf`**. **57 rows, 42 open — 2 P1 · 12 P2 · 28 P3, split 13 `eng` · 29 `training`**.
- **2026-08-02** — pass 14: L1 clean · L2/L3 nothing new. L4–L6 parked (14 open P1/P2). Drained the top inline-buildable item **IB-037** — benchmark grade fields defaulted to `null` in `defaultBenchmarks()` + `onboarding.js:417` but `''` in the live `globalBenchmarks`/`migrate`/`mergeRemote` paths. Normalised the two `null` sites to `''` (grades are strings; numeric benchmarks stay `null`). Verified safe before editing: `normalizeGrade` is `if (!g)` (null/'' equivalent), no strict null/'' grade checks exist, grades aren't rendered raw, and `defaultBenchmarks()` is live (per-plan legacy shape, 6 callers). **Not a schema change** — only new defaults move; existing `null`s stay harmless, so no `SCHEMA_VERSION` bump/migration owed. Touches `js/` (`storage.js` outside the trip-wire-3 functions `mergeRemote`/`newer`/`importJson`, + `onboarding.js`) + `tests/` + `sw.js`; ratchet 1 met by a **green** suite — **247 passed, 0 failed** (246 + a new `[IB-037]` case pinning both shapes agree on `''`), `sw.js`→v63. No issue minted (inline close). Pushed to **`claude/audit-loop-m5zmqf`**. **57 rows, 41 open — 2 P1 · 12 P2 · 27 P3, split 12 `eng` · 29 `training`**.
- **2026-08-02** — pass 15: L1 clean · L2/L3 nothing new. L4–L6 parked (14 open P1/P2). Drained **IB-004** — the Profile "Peaking for" dropdown labelled project `"2-wk rolling taper"`, but `"rolling"` is the only such string in the code and no rolling behaviour exists (`taperWeeksFor`/`buildPhasePattern` treat `trip`/`project` identically, as the onboarding hint already says). Fixed the eng defect — the false promise — by relabelling to `"Project — 2-wk taper (open window)"`; a label-only view change, no trip-wire (not prescription math). Added `[IB-004]` pinning `project === trip` phase patterns so future divergence re-surfaces the label. Left the product question (give project a real rolling taper, or merge the two options — a migration + training call) as a non-blocking future decision, not a silent gap. Touches `js/`+`tests/`+`sw.js`; ratchet 1 met by a **green** suite — **248 passed, 0 failed** (247 + the new case), `sw.js`→v64. No issue minted (inline close). Pushed to **`claude/audit-loop-m5zmqf`**. **57 rows, 40 open — 2 P1 · 12 P2 · 26 P3, split 11 `eng` · 29 `training`**.
- **2026-08-02** — pass 16: L1 clean · L2/L3 nothing new. L4–L6 parked (14 open P1/P2). With the clean inline eng items drained (passes 11–15), the highest-priority open item is now **IB-030** (P3, **G1** — outranks the no-goal P3s) — a *trip-wire, decision-gated* item not yet enqueued, so per the skill this pass **enqueues it for grilling** rather than reaching for a lower phrasing/tooling P3. Verified the defect is real and reachable before minting: on a deload week that is also a Lighter readiness day, a climbing exercise's `prescribedTarget` is cut ×0.6 (`applyDeloadVolume`) then ×0.85 (readiness gate, which runs last) = **×0.51** with no combined floor — the two passes explicitly co-occur (ADR-0015) and nobody chose 0.51. Minted [grill-queue #69](https://github.com/slm37102/simple-climbing-training-planner/issues/69), added a `Queued` row to `grill-queue.md`, recorded routing on the IB-030 row. The decision (is a floor wanted, what value?) plus the `PRESCRIPTION_PASSES` edit make it multi-session. **Docs-only + one issue** — ratchet 1 trivial (no `js`/`css`/`sw.js`/`tests/`); L1 regen was drift-only, restored. Pushed to **`claude/audit-loop-m5zmqf`**. Counts unchanged (IB-030 stays Open): **57 rows, 40 open — 2 P1 · 12 P2 · 26 P3, split 11 `eng` · 29 `training`**.
- **2026-08-02** — pass 17: L1 clean · L3 clean · **L2/survey found a new, previously-unfiled defect** while tracing the readiness path (started as an IB-043 look). Filed **IB-058** (P2, G1): a day with **no readiness check-in** defaults to `{sleep:3,soreness:3,fatigue:3}` (`today.js:493`), which averages 3.0 and scores **Lighter** (`computeReadinessMultiplier` needs `avg ≥ 3.5` for Normal) — so the readiness-gate pass silently down-regulates *every* un-checked-in session (climbing ×0.85 + RPE cap, kg ×0.85), even though `computeReadinessMultiplier(null)` returns Normal. Root cause: the 1–5 pill scale's midpoint (3) sits below the Normal threshold (3.5). Compounded at `today.js:1077`, which *persists* `{3,3,3}` on save, freezing Lighter into the day record + the `readinessTrend` baseline. Broad prescription impact with a genuine "bug vs deliberate-conservative-default" ambiguity → decision-gated, so **enqueued for grilling** ([grill-queue #70](https://github.com/slm37102/simple-climbing-training-planner/issues/70)) rather than fixed inline: minted the issue, added a `Queued` row (ordered below IB-056's G3), recorded routing on the new IB-058 row. Verified: no existing IB/KG row covers it; the monitoring suite treats `{4,4,4}` as its normal baseline, corroborating that 3 is intentionally sub-normal. **IB-043 deferred** — its stacked-callout phrasing fix is cosmetic (P3) and a proper guard needs either a new export seam (sign-off) or a heavy readiness+deload fixture; the readiness dig turned up something far more valuable. **Docs-only + one issue** — ratchet 1 trivial; L1 regen drift-only, restored. Pushed to **`claude/audit-loop-m5zmqf`**. **58 rows, 41 open — 2 P1 · 13 P2 · 26 P3, split 12 `eng` · 29 `training`**.
- **2026-08-02** — pass 18: L1/L2/L3 clean. Adjudicated **IB-031** to closure as **Won't-fix (not a defect)** — the "RPE-drift signal vs `autoAdjust` at cross purposes" framing was already flagged overstated by the deep-audit's own adversarial verifier (§8), and re-verifying against current code confirms it: the four monitoring signals are documented (`monitoring.js:1-8`) as *reactive exception-catchers layered on top of* the proactive thermostat, `rpeDriftSignal` fires only after **two** consecutive above-range sessions at non-increased kg (`kgNotIncreased` screens out the benign "we pushed load up" case), and both it and the thermostat's reason line (`loads.js:177`) point to the **same** ADR-0012 micro-retest — they converge, not contradict. Closing stops the row re-litigating a non-defect each pass (same spirit as an L3 tag). **Docs-only** — ratchet 1 trivial. Pushed to **`claude/audit-loop-m5zmqf`**. **58 rows, 40 open — 2 P1 · 13 P2 · 25 P3, split 11 `eng` · 29 `training`** (IB-031 → Won't-fix).
- **2026-08-02** — pass 19: ran the parked **L4 lens** (G3 / data-integrity — the durability surface) by hand over the sync/merge data-loss paths, since the clean inline eng queue is drained and L4 is the highest-value remaining survey. Checked and **cleared** the `auto`-plan pruning path (a hypothesised offline-data-loss: an `auto:true` startup plan is pruned on merge, but `setDay`/every edit flips `auto=false` on first real data — `storage.js:388,344,278,291` — so only truly-empty placeholders are pruned; no loss) and confirmed `importJson`'s whole-state replace is the intentional S3 destructive-confirm path. **Found one genuine gap → filed IB-059** (P3, G3): `globalBenchmarks.history` is append-only (`saveRetestBenchmarks`, ADR-0014) but `mergeRemote` replaces `globalBenchmarks` **wholesale by LWW** (`storage.js:534-535`), so concurrent multi-device retest snapshots can be lost — the append-only guarantee holds locally, not across sync. Traced the exact LWW loss sequence. Narrow (single-user, rare retests) but real; fix is a union-merge of `history` by date, no decision needed but touches `mergeRemote` (**trip-wire 3**) → multi-session build (not enqueued for grilling — nothing to decide; awaits `/implement`). No existing IB/KG covered it. **Docs-only** — ratchet 1 trivial. Pushed to **`claude/audit-loop-m5zmqf`**. **59 rows, 41 open — 2 P1 · 13 P2 · 26 P3, split 12 `eng` · 29 `training`**.
- **2026-08-02** — pass 20 (**user-authorized trip-wire build**): with the inline queue drained, the user greenlit building the behaviour-neutral trip-wire refactors, which satisfies the trip-wire gate. Closed **IB-053** — `densityRest(weeksLeft, 240)` passed the parameter's own default (`DENSITY_BASE_REST_SEC = 240`) at all three call sites inside `buildThuMain`/`buildSatMain`. Dropped the argument at `:495`/`:618`/`:649` and the speculative `baseSec` parameter (no caller varied it), matching the `densityRest(weeksLeft)` interface `domain-invariants` already documents. L1 clean. Behaviour-neutrality proven the two required ways: **regenerated schedule byte-identical** + suite **248/0** (the `[ADR-0006]` rest-tightening case already pins density behaviour, so no new case). Touches `js/`+`sw.js`; ratchet 1 met by the green suite, `sw.js`→v65. Pushed to **`claude/audit-loop-m5zmqf`**. **59 rows, 40 open — 2 P1 · 13 P2 · 25 P3, split 11 `eng` · 29 `training`**.
- **2026-08-02** — pass 21 (**user-authorized trip-wire build**): built the L4 data-integrity fix **IB-059** — `mergeRemote` replaced `globalBenchmarks` wholesale by LWW, dropping append-only retest `history` snapshots logged on another device. Added a `mergeBenchmarkHistory` helper (union by date, dedupe, sorted) and split the merge: scalar fields still LWW on `updatedAt`, but `history` is now unioned both directions so nothing is lost. Being a `mergeRemote` change (trip-wire 3, the highest-stakes surface), tested deliberately: renamed the stale `[ADR-0014]` "LWW on the whole object" case (its remote was a superset, still green) and added `[IB-059]` — a **newer** remote missing a local-only snapshot must preserve it — plus scalar-LWW and idempotency (no-loop) assertions; the new case fails on the old code (drops to length 2). Suite **249/0**. Touches `js/`+`tests/`+`sw.js`; ratchet 1 met by the green suite, `sw.js`→v66. Pushed to **`claude/audit-loop-m5zmqf`**. **59 rows, 39 open — 2 P1 · 13 P2 · 24 P3, split 10 `eng` · 29 `training`**.
- **2026-08-03** — pass 22: L1 clean (regenerated `sw.js` + schedule, no drift, tree restored) · L2 nothing new (the item touches no invariant-governed code — the callout is pure presentation) · L3 clean (empty). L4–L6 parked (15 open P1/P2, threshold <5, no `--full`). Frontier P1/P2 all decision-gated or trip-wired (IB-014/024 grill-queue; IB-056/058/030 enqueued). Drained the last clean **inline-buildable** eng item, **IB-043** — the target-provenance callout's deload branch. §11 had already established the readiness and ramp branches lead with the final value and dim provenance at `opacity:.6`; only the deload branch still rendered `<s>original</s>value` inline at full opacity, original-first — least legible on exactly the compounded deload+readiness days the crisp-headline-target intent matters most. Applied the sibling branches' own pattern: the cut value is now the bold headline and the struck-through original is demoted into a dimmed `opacity:.6` span, plus a comment refresh. View-only change in `js/views/today.js` — **no trip-wire** (not a prescription/loads/monitoring/sync/schema surface, not a %/RPE constant). Added `[IB-043]` regression through the `renderToday` seam (wk4 Sat deload, normal readiness so the deload branch owns the callout) pinning value-first ordering + the dimmed strikethrough; it fails on the old original-first markup. `/code-review` self-pass: mirrors the two sibling branches, CSS `.callout .v s` styles the strikethrough wherever it sits (no CSS change), values are program-authored (no escaping). Suite **250/0** (249 + 1). Touches `js/`+`tests/`+`sw.js`; ratchet 1 met by the green suite, `sw.js`→v67; schedule byte-identical (no `program.js` change). No issue minted (inline, no downstream consumer). Pushed to **`claude/audit-loop-m5zmqf`**. **59 rows, 38 open — 2 P1 · 13 P2 · 23 P3, split 9 `eng` · 29 `training`**.
- **2026-08-03** — pass 23 (**survey → drain**, L6): with the clean inline eng queue drained, ran the parked **L6 lens** (coverage holes) rather than force a marginal build. It surfaced one concrete hole: of the five views, `today`/`log`/`calendar`/`profile` are each mounted somewhere in the suite, but **`onboarding.js` (the new-plan wizard) had zero coverage** — a throw or empty first screen there strands the athlete with no way to create a plan. Filed it as **IB-060** and drained it the same pass: added `tests/cases/18-onboarding-view.js` (genuinely new area → new numbered file + `index.html` import line) with two `[IB-060]` smoke tests mounting the wizard via `openOnboarding` and driving the shared render/wire loop — open (role=dialog, `1 / 5`, no Back), advance one step (`2 / 5`, discipline selector, Back appears), Cancel (overlay removed, `onDone` not fired). Verified `stepValid()` gates only step 3 (schedule dates), so step 0→1 advance is deterministic. Scoped to open→advance→cancel/teardown, not the `create()` write path. **Tests-only diff** (new case file + one `index.html` import) — no `js`/`css`/`sw.js` touched, so no `--bump` and no schedule regen owed; ratchet 1 met by the green suite — **252/0** (250 + 2). No issue minted (inline, no downstream consumer). Pushed to **`claude/audit-loop-m5zmqf`**. **60 rows, 38 open — 2 P1 · 13 P2 · 23 P3, split 9 `eng` · 29 `training`**.
- **2026-08-03** — pass 24 (**user-authorized trip-wire auto-merge**): with the clean inline queue drained, the user greenlit building trip-wire items with auto-merge to the branch. Note the auth clears the **trip-wire** gate but not the **decision** gate — grill-queue items (IB-014/024/030/056/058) still need a `/grill-queue` decision, so the buildable set is the decision-free trip-wire P3 eng items. Built **IB-052** — the Today flavor badge could *contradict* the prescribed session: it read the week-level `ctx.flavor`, so a hybrid Build sport-parity Thursday (wk8, `2026-06-25`) showed "sport" while the session was force-built as limit bouldering (ADR-0010 `hybridBuildMix`). Could not repoint `session.flavor` — it is a deliberate week-level invariant (`[Phase1 C1]` "constant across the week" + `generate-schedule.mjs` reads it as "\<flavor\>-flavor week"), so `finishSession` now also exposes a **day-level** `session.styleFlavor` and the badge reads `session.styleFlavor ?? ctx.flavor`. Retired IB-042's now-false "may not match the label" tooltip caveat. **Domain-respecting** — the view reads a domain-computed field, no logic moved into it (IB-032 principle). L1 clean · **L2** the `domain-invariants` `finishSession` claim ("same ctx-enrichment skeleton") stays true (it doesn't enumerate fields, so a new one doesn't drift it) · L3 clean. Behaviour-neutrality proven: schedule regenerated **byte-identical** (the `js/program.js` edit is a display field in the finalizer, not a builder/pass/%-RPE constant). Two `[IB-052]` cases pin the split (`styleFlavor=boulder` vs week `flavor=sport`, + the badge reading "boulder"); `[IB-042]` tooltip case updated. Suite **254/0** (252 + 2). Touches `js/`+`tests/`+`sw.js`; ratchet 1 met by the green suite, `sw.js`→v68. No issue minted (inline). Auto-merged/pushed to **`claude/audit-loop-m5zmqf`**. **60 rows, 37 open — 2 P1 · 13 P2 · 22 P3, split 8 `eng` · 29 `training`**.
- **2026-08-03** — pass 25 (survey, no build): reached for the next auto-merge trip-wire item and found the clean queue is genuinely exhausted — every remaining eng item carries a design choice, not a mechanical build. Examined **IB-055** (the most "just build it" candidate) for an auto-merge and **found its premise wrong**: a content-hash `CACHE` does *not* retire the CI guard, because the hash only changes if the generator is re-run — a `js/` edit with a forgotten `sw.js` regen still produces "source changed, CACHE unchanged", the exact stale-client shape the guard exists for. Sharpened the row in place (a naive "simplify" here would reintroduce a G3-adjacent risk) and re-tagged it build-gated on a small deploy-pipeline decision, not auto-mergeable. Surveyed the rest: **IB-035** (CLI test runner) needs a Playwright-provisioning decision; **IB-015** is a UI feature (personalization editor); **IB-034/036** are architectural refactors that L5/`/improve-codebase-architecture` owns. **Docs-only** (sharpened one row + this log line) — ratchet 1 trivial. Pushed to **`claude/audit-loop-m5zmqf`**. Reported the wall to the user for direction. **60 rows, 37 open — 2 P1 · 13 P2 · 22 P3, split 8 `eng` · 29 `training`**.
- **2026-08-05** — pass 27 (**survey → drain**, L6): the designated branch's PR (#71) had merged to `main`, so per the merged-PR rule the branch was restarted fresh from `origin/main` (`91c71e9`) and this pass stacks on it. **L1 clean** (regenerated `sw.js` + schedule in `$WT`, no drift, tree restored) · **L2 clean** — verified the invariant claims against code (`SCHEMA_VERSION=6`, `DOUBLE_BLOCK_THRESHOLD=20`, `densityRest(weeksLeft)` single-arg, `holdProgressionFor` cause-based, content-hash CACHE with no `--bump`), all literally true; the `domain-invariants` skill already names IB-056's taper gap · **L3** prints only **ADR-0017**, which is the Decided-but-unbuilt finger-tweak clamp (IB-014, awaiting `/to-spec → /to-tickets`) — no test can pin a feature not yet in the code, so it's IB-014's known state, not a new hole; it will re-print until that build lands. With the clean inline eng queue drained (passes 25/26) and the frontier P1/P2 all decision-gated (IB-014 built-pending, IB-024 grill-queue, IB-056/058/030 enqueued), ran the parked **L6 lens** (coverage) by hand — the established move when the shallow queue is empty (cf. passes 19/23). It surfaced a genuine hole: **the pre-v5 schema-migration chain had no round-trip test** — only v5→v6 was pinned, while the v3→v4 structural wrap (the path a returning user hits, and where a regression silently drops logged history) was bare. Filed **IB-061** and drained it the same pass: two `[IB-061]` characterization cases via the LocalStorage load door (`importJson` rejects plan-less states, so it can't reach the pre-v4 path), pinning that the v3→v6 chain preserves logged days/actuals + settings + benchmarks and is idempotent. Behaviour verified in a Node `localStorage`-shim probe before writing, so the test pins real behaviour. Recorded (not fixed) an observation the probe turned up — legacy per-plan `benchmarks.history` isn't promoted to `globalBenchmarks.history` at v4→v5 — as a trip-wire-4 gap for a future look, and asserted only the v6 `Array.isArray` invariant so nothing questionable is baked in. **Tests-only diff** (new cases in `tests/cases/01-inputs-storage.js`) — no `js`/`css`/`sw.js` touched, so no regen/`--bump` owed; ratchet 1 met by the **green suite — 256/0** (254 + 2). No issue minted (inline close, no downstream consumer). Per the branch directive, pushed to **`claude/audit-loop-m5zmqf`**. **61 rows, 36 open — 2 P1 · 13 P2 · 21 P3, split 7 `eng` · 29 `training`**.
- **2026-08-03** — pass 26 (**user delegated the decision, then auto-merge**): built **IB-055** after the user chose option (a) "decide the pipeline question and build it." Decision made and recorded on the row: **content-hash CACHE + keep the CI guard + drop `--bump`**. Pass 25 had worried the guard needed reworking; on building it that proved false — the guard's rule ("js/`css/` changed ⟹ CACHE must change") holds unchanged under content-hashing, so `.github/workflows/firebase-deploy.yml` was untouched. `tools/generate-sw.mjs` now derives the key from a sha256 of the shell assets' bytes; proved idempotent + deterministic + cache-busting with a controlled edit/restore. Retires the counter, the `--bump` flag, and the "bump once per pass" rule; fixes the silent same-version collision; and — a bonus — makes **L1** catch a forgotten regen locally (it was blind to CACHE staleness before). Updated the **live** `--bump` instructions across `CLAUDE.md` + the `domain-invariants`/`audit-loop`/LENSES skills + `copilot-instructions.md`; left dated historical records untouched. L1 clean · L2 the `domain-invariants` sw.js bullet was itself one of the live instructions, now corrected (no residual drift) · L3 clean. Diff touches `sw.js` (+ `tools/` + docs/skills), no `js`/`css` logic → CI guard won't even fire on this commit; ratchet 1 met by the green suite **254/0**. No issue minted (inline). Auto-merged/pushed to **`claude/audit-loop-m5zmqf`**. **60 rows, 36 open — 2 P1 · 13 P2 · 21 P3, split 7 `eng` · 29 `training`**.
- **2026-08-05** — pass 28 (**survey → enqueue**): pass 27's IB-061 commit (`8642c1b`) was on `origin/claude/audit-loop-m5zmqf` but never merged to `main`, so this pass stacks on it (branch directive: develop on the designated branch, don't merge to main) — I'm pass 28, not 27. **L1 clean** (regenerated `sw.js` + schedule off `origin/main` in a worktree, `git diff --quiet` exit 0, tree restored; IB-061 only added test cases, so the shell/schedule are unaffected) · **L2 clean** — spot-checked the invariant constants against code: `DOUBLE_BLOCK_THRESHOLD=20`, `DELOAD_VOLUME_MULTIPLIER=0.6`, `SCHEMA_VERSION=6`, `clampCycleWeeks` 8–40, and the seven-pass `PRESCRIPTION_PASSES` order (anti-style-cue → base-volume-ramp → deload-cut → taper-cut → …) all literally true · **L3** prints only **ADR-0017** again — the Decided-but-unbuilt finger-tweak clamp (IB-014), no test owed until that build lands. L4–L6 parked (14 open P1/P2 ≥ 5, no `--full`). Frontier walk: the highest not-already-routed item is **IB-029** (P2, training, G1+G3) — IB-024/056/058/030 are all already in the grill queue, IB-014 is decided-awaiting-build. IB-029 is a not-yet-enqueued training trip-wire, so per the skill this pass **enqueues** it. Verified it's sharp and not superseded: its **honest-labeling** half is already closed (KG-C7, 2026-07-23), but the audit's actual recommendation — **self-calibrate** the autoregulation multipliers from the athlete's logs, the loop `loads.js:21-22`/`:46` explicitly promise ("Tune them from the athlete's own logs via the KG-A4 monitoring signals") yet nothing ever built — is undecided and P2. Minted **[grill-queue #75](https://github.com/slm37102/simple-climbing-training-planner/issues/75)** (labelled `grill-queue`, claim protocol in body), added the `Queued` row to [grill-queue.md](grill-queue.md), and sharpened the IB-029 backlog row to split the closed labeling half from the open self-calibration half + a separable sub-question (widen/suppress the RPE thermostat in the noisy sub-maximal ARC/60-60 regime). **Docs-only** (grill-queue.md + backlog + this log line) — no `js`/`css`/`sw.js`/`tests/` touched, so ratchet 1 is trivial and no regen/schedule owed. No inline build ticket minted (the *decision* is #75; a build ticket comes downstream after `/grill-queue` settles it). Pushed to **`claude/audit-loop-m5zmqf`** per the branch directive. **61 rows, 36 open — 2 P1 · 13 P2 · 21 P3, split 7 `eng` · 29 `training`** (enqueue routes, doesn't close — counts unchanged from pass 27).
- **2026-08-05** — pass 29 (**survey → drain**): stacks on pass 28. **L1 clean** (regenerated `sw.js` off the current tree, no source drift until the build itself; schedule not owed — no `program.js` change) · **L2 clean** — re-confirmed the constants (`DOUBLE_BLOCK_THRESHOLD=20`, `DELOAD_VOLUME_MULTIPLIER=0.6`, `SCHEMA_VERSION=6`) unchanged · **L3** unchanged (only ADR-0017, awaiting IB-014 build). L4–L6 parked (13 open P1/P2 ≥ 5 after the close). Frontier walk: the top not-routed items are P2 `training` (grill-queue candidates) and the enqueued eng items; the highest-priority **inline-buildable** (no-trip-wire) item is **IB-015** (P3, eng, G1), so per the division of labour (this loop drains the eng backlog) it was the pass's item. Built it: the KG-A10 anti-style fields (`dominantStyle`/`dominantAngle`) were schema-collected and read by the prescription but had no editor, pinning the cue to the crimp/slight-overhang default. Added grip+angle `.pill` selectors to the Profile Benchmarks card (`js/views/profile.js`), committed on Done via the existing `Storage.setGlobalBenchmarks`. **View-only, no trip-wire** — no `program.js`/`loads.js` touched, the editor only realizes decided behaviour. `/code-review` inline (Standards: pill idiom, no `escHtml` owed on the fixed program vocabulary, merge-save preserves siblings; Spec: hits exactly IB-015's "no editor" half, no scope creep). `[IB-015]` regression pins mount→Edit→pick sloper/roof→Done→persisted→`styleNote` flips to the new opposite. Touches `js/`+`tests/`+`sw.js`; ratchet 1 met by the **green suite — 257/0** (256 + 1), `sw.js`→`climb-planner-8fb6f7813406`. No issue minted (inline build, no downstream consumer). Pushed to **`claude/audit-loop-m5zmqf`** per the branch directive. **61 rows, 35 open — 2 P1 · 13 P2 · 20 P3, split 6 `eng` · 29 `training`**.
- **2026-08-05** — pass 30 (**user-authorized; principle question resolved**): asked the user whether a Playwright-based dev/CI runner was acceptable despite the repo's "no test framework" line; they said yes, so built **IB-035**'s headline deliverable — `tools/run-tests.mjs`, the first way to run the browser suite from a terminal/CI. It serves the repo, drives `/tests/` in headless Chromium, scrapes the harness `#summary`, prints the summary + failing case names, exits **0 green / 1 fail**. Kept faithful to the ethos: **not a framework** (the harness + `tests/cases/*` are untouched — it only *drives* them), and the app keeps zero runtime deps — Playwright is optional dev tooling, resolved from a local or global install (`npm root -g` fallback) with an explicit install message + non-zero exit if absent. Validated end-to-end **both directions**: green → `257/0` exit 0; a temporary failing case → exit 1 with the case named, then restored clean. Retired the "no CLI runner" claims in `CLAUDE.md` + the `test` skill (kept "no test framework" — still true). Coverage sub-clause **dispositioned, not dropped**: sync is now covered (IB-059/061), there is no chart module to cover, and sw.js runtime caching stays a low-value future L6 pick. L1/L2/L3 clean. Diff touches only `tools/` + docs (no `js`/`css`/`sw.js`/`tests`) → ratchet 1 trivial; the runner was itself exercised to 257/0. Drains the last non-architectural eng item — the only open eng left (IB-034/036) are L5 refactors, one of which the deep-audit itself rated against-principle. Auto-merged/pushed to **`claude/audit-loop-m5zmqf`**. **61 rows, 33 open — 1 P1 · 13 P2 · 19 P3, split 5 `eng` · 28 `training`**.
- **2026-08-06** — pass 31 (**survey → enqueue**, ran deep lenses by hand; arg "loop until finish"): **stale-base correction first** — the stage-1 worktree was opened off `main` (`91c71e9`), but the designated branch is **4 commits ahead** (`81e6826`, carrying passes 27–30: IB-061/029/015/035), so `main` is stale; caught it when `tools/run-tests.mjs` (IB-035, pass 30) was absent from the worktree. Same trap pass 9 hit: the `git log origin/main..main` check is empty when `main` is *behind*. `git worktree list` showed only the primary checkout (on the designated branch at the true tip, clean) + the stale worktree — **no parallel session in my tree**, so per pass 9's precedent the stale worktree was set aside and all survey/edits re-run against the **primary checkout at the true tip**. (A parallel *grill* session does exist — branch `claude/grill-section-ymswpe`, decisions lane — but on its own checkout, not in my `git worktree list`, so no shared-tree collision.) **L1 clean** (regenerated `sw.js` content-hash + schedule, `git diff --quiet` exit 0, restored) · **L2 clean** (`SCHEMA_VERSION=6`, `DOUBLE_BLOCK_THRESHOLD=20`, `DELOAD_VOLUME_MULTIPLIER=0.6`, `BUILD_FRACTION_OF_REMAINING=0.33`, `holdProgressionFor` all literally true) · **L3** only ADR-0017 untagged — expected, it's IB-014's *decided-but-unbuilt* finger-tweak state (no code yet), not drift. With the clean inline eng queue drained (passes 25/30 reported the wall), ran the parked **L4 + L6 lenses by hand via sub-agents** (highest-value survey when the queue is dry — precedent passes 19/23), re-validating every finding against the true tip (the agents read the stale worktree; the domain/sync files are byte-identical main→tip, and both storage-coverage holes were re-confirmed absent on the tip). **Four new findings filed:** L6 → **IB-062** (`duplicatePlan` deep-clone behind a live Profile button, zero coverage — a dropped `clone.days={}` silently bleeds the source's log into the copy), **IB-063** (v2→v3 `parseLegacyActual` string-parse untested — the step IB-061 *explicitly* left uncovered), **IB-065** (`deletePlan` guard/reassign untested, minor); L4 → **IB-064** (P1, G3 — readiness **"Push" ×1.05 raises finger/pull kg +5% on deload/retest/taper/pain-amber weeks, bypassing `holdProgression`**, which gates only the +2.5% step; verified against `loads.js:194/210-213/220`). **Pass item = enqueue IB-064** — the highest-priority new finding, a not-yet-enqueued trip-wire (trip-wire 2, load chain) + decision-gated item, so per the skill this pass *enqueues* it rather than draining a lower inline P3: minted [grill-queue #76](https://github.com/slm37102/simple-climbing-training-planner/issues/76) (label `grill-queue`, claim-by-comment, no assignee), added a `Queued` row + a Frontier bullet + routing on the IB-064 row. **Confirmed IB-064 is not subsumed** by the just-decided IB-056 ([#68](https://github.com/slm37102/simple-climbing-training-planner/issues/68) closed 2026-08-05, ADR-0007 addendum / KG-B17): that decision scoped its fix to "suppress *only* the +2.5% step" and kept the readiness lever running, so this facet is deferred to #76 by design. IB-062/063/065 stay Open for subsequent inline passes this session. **Docs-only + one issue** — no `js`/`css`/`sw.js`/`tests` touched, so ratchet 1 is satisfied trivially; L1's regen was drift-detection only (in the set-aside worktree), and the primary tree carries only the two docs edits. Pushed to **`claude/audit-loop-m5zmqf`**. **65 rows, 37 open — 2 P1 · 13 P2 · 22 P3, split 9 `eng` · 28 `training`**.
- **2026-08-06** — pass 32 (drain, stacks on pass 31): L1 clean (regenerated `sw.js` content-hash + schedule, `git diff --quiet` exit 0, restored) · L2/L3 unchanged from pass 31 (same tip + a tests-only diff). Drained the top inline-buildable item filed last pass — **IB-062**, the `Storage.duplicatePlan` coverage hole. Added one `[IB-062]` case to `tests/cases/01-inputs-storage.js` pinning the deep-independent clone: fresh id, `'Base (copy)'` default name, carried-over `focus`, **empty `days`**, and cross-plan isolation (a write into the copy doesn't touch the source). **Red-checked before shipping** — commenting out `clone.days = {}` in `js/storage.js` turned exactly this case red (257/1) and nothing else, then restored; so the test catches the real silent-corruption regression, not a constant. Tests-only diff (no `js`/`css`/`sw.js` touched — `sw.js` SHELL excludes `tests/`, so no `--bump`/regen owed and the schedule stays byte-identical), so ratchet 1 is met by the **green suite 258/0** (257 + 1). No issue minted (inline close, no downstream consumer). Pushed to **`claude/audit-loop-m5zmqf`**. **65 rows, 36 open — 2 P1 · 13 P2 · 21 P3, split 8 `eng` · 28 `training`**.
- **2026-08-06** — pass 33 (drain, stacks on pass 32): L1 clean (regenerated `sw.js` content-hash + schedule, exit 0, restored) · L2/L3 unchanged (same tip, tests-only diff). Drained **IB-063** — the v2→v3 `parseLegacyActual` string-parse hole IB-061 explicitly scoped out. Added one `[IB-063]` case seeding a `version:2` state with a legacy string `actual` (`"5x2 @ 62kg RPE 9"`) through the real `Storage.init()` migrate door and asserting the parse into `{sets,reps,kg,rpe,raw}` plus the day surviving to v6. **Red-checked** — nulling the `@…kg` regex turns exactly this case red (258/1) and nothing else, then restored. Together with IB-061 this now covers the whole v2→v6 load-path migration chain. Tests-only diff (no `js`/`css`/`sw.js` — no `--bump`/regen owed, schedule byte-identical), ratchet 1 met by the **green suite 259/0** (258 + 1). No issue minted (inline close). Pushed to **`claude/audit-loop-m5zmqf`**. **65 rows, 35 open — 2 P1 · 13 P2 · 20 P3, split 7 `eng` · 28 `training`**.
- **2026-08-06** — pass 34 (drain, stacks on pass 33): L1 clean (regenerated, exit 0, restored) · L2/L3 unchanged. Drained the last filed inline item from pass 31's survey — **IB-065**, the `Storage.deletePlan` guard/reassign hole. Added one `[IB-065]` case pinning both behaviours: deleting the **active** plan moves `activePlanId` to a survivor, and deleting the **last** plan throws `'Cannot delete the only plan.'` without deleting it. **Red-checked** — disabling the reassignment line turns exactly this case red (259/1), then restored. Tests-only diff (no `js`/`css`/`sw.js` — no `--bump`/regen owed), ratchet 1 met by the **green suite 260/0** (259 + 1). No issue minted (inline close). Pushed to **`claude/audit-loop-m5zmqf`**. This drains every inline item pass 31's L4/L6 survey filed. **The inline eng queue is empty again:** the six remaining open eng rows are all either enqueued for grilling (IB-030 #69, IB-056 #68, IB-058 #70, IB-064 #76 — decision-gated) or architectural / L5-owned (IB-034, IB-036) — none is a clean inline build. **65 rows, 34 open — 2 P1 · 13 P2 · 19 P3, split 6 `eng` · 28 `training`**.
- **2026-08-06** — pass 35 (**final survey → drain**, L6): with the pass-31 findings drained and the inline queue empty (pass 34), ran a final differently-angled **L6 sub-agent** over the modules not yet deeply covered (warmup, limiter, replan, dates, exercise-inputs edges, storage non-trip-wire methods) to honestly test whether the loop is finished. It found the queue *not quite* exhausted: one genuinely valuable, one modest, rest covered/trip-wired/make-work. L1/L2/L3 clean (regenerated artifacts exit 0, restored; invariants unchanged; only ADR-0017 untagged = IB-014 unbuilt). **Filed IB-066** (P3, G3 — `Warmup.forSession` warm-up *content* routing unpinned; `08-drills.js` asserts only `.skillDrills`, so the `mon-hangboard` progressive-finger-warm-up branch, the app's pulley/tendon injury guard, is unprotected against a `program.js` sessionId rename) and **IB-067** (P3 — first-run empty-state renders never mounted). **Declined** the survey's third candidate (a `Storage.archivePlan` round-trip) — cosmetic plan-switcher filter, single-user, no data/safety/training impact; per `CLAUDE.md`'s ongoing-maintenance test it doesn't earn a row. Drained the valuable one — **IB-066**: probed the real session IDs first (`mon-hangboard-base` / `thu-projecting-base`) so the assertions pin true behaviour, added a `[IB-066]` case deriving both sessions from `Program` and asserting the hangboard vs climbing warm-up content (+ mutual exclusivity + rest→empty). **Red-checked** — dropping the `mon-hangboard` prefix fails exactly this case (260/1). Tests-only diff (no `js`/`css`/`sw.js` — no `--bump`/regen owed), ratchet 1 met by the **green suite 261/0** (260 + 1). Stale worktree from pass 31 removed (ratchet 5). No issue minted (inline close). Pushed to **`claude/audit-loop-m5zmqf`**. **67 rows, 35 open — 2 P1 · 13 P2 · 20 P3, split 7 `eng` · 28 `training`**.
- **2026-08-06** — pass 36 (drain, **closes the inline queue**): L1/L2/L3 clean. Drained the last filed inline item — **IB-067**, the first-run empty-state renders. Added one `[IB-067]` case mounting `renderToday` + `renderCalendar` on a fresh (unconfigured) plan and asserting both "configure your plan" first-run cards render. **Red-checked** — bypassing the Today `if (!start)` guard fails exactly this case (261/1). Tests-only diff, ratchet 1 met by the **green suite 262/0** (261 + 1). No issue minted (inline close). Pushed to **`claude/audit-loop-m5zmqf`**. **This closes the inline engineering queue** for the "loop until finish" run: passes 31–36 filed 6 findings (IB-062…067), enqueued the one G3 prescription defect (IB-064 → #76), and built the five clean inline coverage/robustness items. Every remaining open item is now decision-gated (IB-024/030/056/058/064 in the grill queue — worked via `/grill-queue`), architectural / L5-owned (IB-034/036 — `/improve-codebase-architecture`), or a training-content gap awaiting graduation. **No clean inline-buildable eng work remains.** **67 rows, 34 open — 2 P1 · 13 P2 · 19 P3, split 6 `eng` · 28 `training`**.
- **2026-08-06** — pass 37 (**drain — and pass 36's "queue exhausted" claim was wrong**): opened the worktree at the **designated-branch tip** this time (`f3c7478`), not `main` — main is 10 behind, the stale-base trap passes 9 and 31 both hit. L1 clean (regenerated `sw.js` + schedule in `$WT`, exit 0, restored) · **L2 clean, and productively so** — walking `training-philosophy.md`'s claims against code *verified* its phase-duration numbers exactly (12-wk comp `buildPhasePattern` = 6/3/2/1 = 50/25/17/8%, matching the doc's "~50/25/17/8") while exposing that the *intensity* claim built on them was false · L3 unchanged (only ADR-0017, IB-014 unbuilt). **Correction to pass 36:** it declared "no clean inline-buildable eng work remains" by reading the ledger's Kind column instead of the source findings. Testing that claim against the frontier P2 — **IB-001**, filed `training` — showed [deep-audit §1](deep-audit.md) had *already adjudicated it*, adversarially verified: "a documentation over-claim, not an unsafe prescription", relabel the doc and **"do NOT … lower Base-phase RPE to fit the 80/20 label"**, "no training cost to relabelling." Both branches point one way, so no decision was outstanding and the fix is **docs-only, no trip-wire**. Re-kinded `training` → `eng` (stated on the row, not silently) and drained it: rewrote the section as "Year-level shape **and intensity distribution**", keeping the true phase-duration claim and replacing the false 80/20 *intensity* claim with the honest **concurrent / threshold-pyramidal** model plus its genuinely polarized aerobic sub-component — every rung cited from source, and **Base Saturdays confirmed to alternate `sat-arc` RPE 4–6 / flash pyramid RPE 6–7.5 by walking every Base Saturday** rather than assuming (the pass-7 lesson: a plausible-but-wrong assertion is worse than none). Quantified the crux — a Base week is **one easy + two RPE 8–9 loaded sessions**, ~half hard, not 80% easy. Carried the audit's reader-hazard into the doc explicitly ("do not add hard work to 'fill the 20%'"), which is the real G1/G3 exposure the mislabel created. Swept the repo for wider drift (IB-048/050 lesson): confined to that one section — the research report merely describes the literature, and it **supplies bonus evidence** (Open Questions: polarized models "lack climbing-specific research"), now cited. Ratchet 4 clear (no ADR asserts 80/20; nothing in `js/` consumes the label) → no addendum owed; no `KG-*` graduation owed either, since no prescription changed — recorded explicitly so it doesn't read as a skipped step. **Docs-only** diff (no `js`/`css`/`sw.js`/`tests`) → ratchet 1 satisfied trivially, no suite run or regen owed; L1's regen was drift-detection only and restored. No issue minted (inline close). **Lesson for the loop: "inline queue exhausted" must be judged from the source findings' recommendations, not the ledger's Kind column** — a `training` row whose audit already adjudicated the direction is inline work. Worktree removed (ratchet 5). Pushed to **`claude/audit-loop-m5zmqf`**. **67 rows, 33 open — 2 P1 · 12 P2 · 19 P3, split 6 `eng` · 27 `training`**.
