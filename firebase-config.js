// Replace these with your own Firebase project's web config (see README).
// Public values are safe to commit; security is enforced by Firestore rules.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  appId: "REPLACE_ME"
};

// Set to false to disable Firebase entirely (local-only build).
export const SYNC_ENABLED = true;
